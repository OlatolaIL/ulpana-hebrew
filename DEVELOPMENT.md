# Разработка открытой беты

## Отдельная задача пользователя: телефонные сценарии всех 100 уроков — 22.09.2026

Codex выполняет ограниченное поручение по Этапу 6 в ветке `codex/stage6-phone-contracts`, отдельный worktree `C:/Users/azrie/Documents/antigravity/goofy-maxwell-phone-stage6`, база `f13d586756a45be3050cd10cf33ee06b3dda4cf5`. Это не передача всего проекта и не отмена роли Gemini. Пользователь вручную передаст Antigravity ссылку на результат; прямых сообщений, push, merge и выпуска нет.

Исправлена общая причина инверсий роли урока 7: явные контракты всех 100 звонков, серверные цели/факты, native system/history Gemini, контекстная память, отдельное завершение и оценка по целям. Словарь всех предыдущих уроков и текущего; частотные слова разрешены пользователем, временные рамки 1–35 / 36–50 / 51–100 сохранены. Изменены только телефонные данные, обработчики, связанные проверки и документация; мастер-словарь и материалы других этапов не редактировались.

Задание: `project-control/tasks/stage6-phone-contracts.md`. Фактические результаты и ограничения: `project-control/results/stage6-phone-contracts-result.md`. Статус — ревью Antigravity, не коммерческая или языковая приёмка. Известные ограничения: модельная семантика, узкие детерминированные фильтры, отсутствие полного голосового E2E; текущие результаты npm ci, lint, audit и живых моделей смотреть в отчёте, не брать из исторических разделов.

> Актуальное поручение пользователя от 14 сентября 2026: дальнейшая разработка и роль архитектора переданы Gemini. ChatGPT прекратил самостоятельное ведение проекта; пользователь переносит инструкции Gemini вручную. Старое поручение Codex работать до 08:00 и ожидать его приёмку отменено этой передачей. Исторические проверки ниже сохраняют силу только в указанной области. Попытка поставить автоматизацию Codex 08-00 на паузу вернула Transport closed; техническая остановка расписания не подтверждена. При старом heartbeat не возобновлять работу вопреки этой передаче.

Обзор состояния проекта: [этапы, задачи, доказательства и решения](project-control/overview.md). Источник статусов — project-control/state.json; интерактивная панель является его снимком. После принятого результата архитектор обновляет источник и обзор.

Сейчас выпускается бесплатная бета: гостям доступны первые два урока, после входа — все сто. Платежи и промокоды не включать без отдельной реализации и проверки биллинга.

## Совместная работа IDE

Одна задача — одна ветка и отдельный checkout/worktree. Одновременно редактировать один каталог из двух IDE нельзя: это смешивает изменения и делает результаты проверок недостоверными. Перед передачей задачи другой IDE фиксировать список принадлежащих ей файлов, ожидаемое поведение и способ проверки. После её завершения смотреть diff и самостоятельно запускать проверки.

Роли и общие правила определены в AGENTS.md: Gemini ведёт проект, Codex участвует по отдельному поручению пользователя. Прежняя схема «ChatGPT — архитектор, Gemini — исполнитель» отменена. Изменения авторизации, синхронизации, оценивания и схемы данных требуют отдельного ревью. Исправления от модели нельзя принимать только по её отчёту.

Перед началом работы с кодом смотреть git status, AGENTS.md, раздел рабочего контекста в DEVELOPMENT.md и RELEASE_READINESS.md. Текущий этап, открытые вопросы и краткую передачу контекста хранить в разделе рабочего контекста этого файла; подтверждённые результаты проверки готовности — в RELEASE_READINESS.md. Очищать контекст по расписанию не требуется; короткий журнал должен позволять продолжить работу без истории переписки.

## Проверки

Использовать Node.js 24 и зависимости из package-lock.json.

```sh
npm ci
npm run typecheck
npm test
npm run build
npm audit --audit-level=high
npm run lint
```


Полный lint пока содержит технический долг; workflow показывает его отдельным предупреждением. Проверка типов, тесты, сборка, аудит зависимостей и lint трёх критических компонентов обязательны. Правила не отключались для сокрытия ошибок. Workflow подготовлен локально; успешный запуск GitHub Actions можно утверждать только после реального прогона.

Интеграционные тесты требуют отдельную PostgreSQL: 127.0.0.1:55433, база ulpana_beta_test. Перед npm run test:db установить TEST_DATABASE_URL для этой базы. Тесты отказываются запускаться на другом адресе и очищают собственные данные. Никогда не направлять их в рабочую БД.

Перед выпуском пройти интерфейс гостем и двумя тестовыми учениками: пропуск/ошибка/повтор упражнения, сохранение после перезагрузки, вход/выход, изоляция аккаунтов, недоступный ИИ, мобильные подсказки, клавиатура. Голосовые сценарии проверять отдельно в тихой обстановке с тестовым голосом.

## Секреты и данные

Секреты задаются в окружении размещения и не попадают в исходники, URL, журнал или отчёт. Авторизацию подписывать новым независимым JWT_SECRET. Telegram webhook должен проверять секретный заголовок. Записи голоса хранить только в закрытом R2/S3; их выдаёт защищённый API.

Сохранять резервную копию базы перед выпуском. Проверять восстановление отдельно. На время беты ограничивать общий бюджет ИИ и расходы в кабинете провайдера. Смена версии или перезапуск сервера не сбрасывает лимит в БД.

## Регламент выгрузки на боевой сервер (Deployment Flow)

Выгрузка изменений на боевой сервер (production) строго регламентирована:
1. **Полномочия:** В соответствии с `AGENTS.md`, выгрузка выполняется **исключительно по прямому поручению пользователя**. Самовольная публикация запрещена.
2. **Обязательные предварительные проверки:** Перед коммитом и выгрузкой должны успешно проходить:
   ```sh
   npm test
   npm run build
   npm run typecheck
   ```
3. **Контроль состояния рабочей копии:** Выполнить `git status`, убедиться в отсутствии непредусмотренных или временных файлов и корректности затронутых изменений.
4. **Коммит и отправка:**
   - Зафиксировать изменения информативным коммитом (Conventional Commits: `fix(...)`, `feat(...)` и т.д.).
   - Выполнить `git push origin main`. Удалённый репозиторий GitHub (`OlatolaIL/ulpana-hebrew`) настроен на приём изменений по защищённому токену доступа (PAT).
5. **Автоматический деплой на Vercel:** Vercel подключён к репозиторию GitHub и автоматически собирает каждый пуш в ветку `main`. Результат обновляется на `https://ulpana-hebrew.vercel.app`.

## Единый мастер-словарь Pealim (Single Source of Truth)

- **Источник истины:** `src/data/pealimMasterDictionary.json` (содержит академические данные с огласовками, כתיב מלא, корнями, семьями корней и таблицами спряжения для всех слов курса и колод).
- **Синхронизация глаголов и мастер-лексикона:** `src/lib/verbConjugations/database.ts` (спряжения глаголов), `src/data/pealimMasterLexicon.json` (полный лексикон всех 10 286 слов всех частей речи с огласовками и аудио Pealim) и `src/data/pealimRootsIndex.json` (индекс семей корней) генерируются строго автоматически из мастер-словаря командой:
  ```sh
  node scripts/fetch_pealim_dictionary.cjs --sync-db
  ```
  Прямое ручное редактирование этих файлов строго запрещено.
- **Удаление ручных пресетов:** старые разрозненные файлы `src/lib/rootFamiliesData.ts` и `src/lib/verbConjugations/rootPresets.ts` полностью упразднены и удалены. Создавать новые списки пресетов корней в кодовой базе запрещено.
- **Пополнение новыми словами:** выполняется централизованно через скрипт `node scripts/fetch_pealim_dictionary.cjs`.

## Рабочий контекст «Ульпан Алеф»

Актуально на 14 сентября 2026, 06:34 Asia/Jerusalem. Этот раздел описывает текущее состояние; подробный исторический снимок сохранён без изменений в [development-history-2026-09-14-0620.md](project-control/history/development-history-2026-09-14-0620.md). Старые формулировки «текущая задача» в архиве не являются новыми поручениями. Обновлять этот раздел на месте, а подробные доказательства хранить в project-control/results и evidence.

### Поручение и пределы

ChatGPT — архитектор и независимая приёмка; Gemini — разработчик в Antigravity. Пользователь разрешил самостоятельную работу до **14 сентября 08:00 Asia/Jerusalem (05:00 UTC)**. Новая команда остановиться прекращает её немедленно. В 08:00 не начинать изменения, составить итог и приостановить существующую автоматизацию **08-00** штатным инструментом. Она работает каждые 15 минут; старый текст поручения не учитывает последующие приёмки — руководствоваться этим разделом и state.json. Прежняя попытка изменить описание автоматизации вернула Transport closed; это не означает остановки расписания.

Цель — коммерчески пригодный продукт с современным нормативным ивритом (с приоритетом современного полного написания כתיב מלא, согласованными огласовками при showNikkud, отсутствием усеченных форм без никуда), согласованными транскрипциями, учебными целями и всеми механиками. Общие правила проекта находятся в AGENTS.md. Пользователь принимает бизнес-решения; состав коммерческой версии пока не определён. Оплату, бизнес-модель, production и публичный выпуск без отдельного поручения не менять. Не создавать новые задачи Codex и не возобновлять перегруженный прежний диалог.

### Принято и уже проверено

Локальная **main: b2dd5f21eaace35604285d011e889350f1729679**. Семь исправлений приняты независимо и включены локально:

| Результат | Последний SHA исправления | Ревью в project-control/results |
|---|---|---|
| Женские телефонные подсказки 2–5, скрытие ответа аудирования | 0674b16 | pilot-mechanics-01-05-review.md |
| Сохранение нормативного у- нормализатором/генератором | 6b397f6 | transcription-preserve-vav-review.md |
| Род и времена в оценивании сочинения | 8c0cc22 | essay-evaluation-gender-review.md |
| Сочинения 3–5 по целям уроков | 502d065 | pilot-essay-alignment-03-05-review.md |
| Выбор родовых вариантов и размещение подсказок сочинения | e8148cc | essay-hint-variant-selection-review.md |
| Числительные, упражнения и телефон покупок урока 5 | d413f94 | lesson-05-practice-alignment-review.md |
| Нормативный союз в промптах/phone sanitizer/передаче текста TTS | b2dd5f2 | runtime-normative-vav-review.md |

На последней принятой базе: **91/91 тест, типы, сборка 33/33, audit и обязательный lint прошли**. Существующий lint-долг отдельно зафиксирован; полного нулевого lint нет. Реальные POST проверялись с перехватом провайдера; физическое звучание, живой ИИ и полная академическая приёмка курса этим не подтверждены. Не повторять завершённые проверки без новой причины. Развёрнутых изменений не заявлять.

**15.09.2026 — Исправления качества звонка (коммит fcf8d40):**
Пользователь сообщил о трёх проблемах в звонке «Ноам, Урок 1, Этап 6»: долгий первый ответ ИИ, несоответствие уровня разговора, мало времени на ответ.
- Диагноз: `GROQ_MODEL` не задан в env → дефолт `openai/gpt-oss-120b` (reasoning, latency 5–15 с). Gemini-ключ не задан. Сценарий урока 1 корректный (`BESPOKE_PHONE_SCENARIOS[1]`).
- Исправлено: дефолт модели в `src/lib/aiModels.ts` → `llama-3.3-70b-versatile` (~1–2 с); пауза перед авто-отправкой в `usePhoneCall.ts` для уроков 1–5 увеличена с 1500 до 2000 мс.
- TypeScript чист (0 ошибок). Уровень несоответствия языка также объясняется reasoning-моделью; ожидается улучшение с llama.
- Нужно: если на боевом Vercel задан `GROQ_MODEL` — убедиться что он не `gpt-oss-120b`. Если переменная не задана — деплой подхватит новый дефолт автоматически.

**16.09.2026 — Движок политик доступа, Telegram-канал @ulpana_il и управление в /admin:**
- Реализована серверная проверка подписки на Telegram-канал @ulpana_il (`/api/auth/telegram/check-channel`) через Telegram Bot API с сохранением в БД Postgres (`ulpana_users.is_channel_subscriber`, `channel_verified_at`).
- Создан движок политик доступа `src/lib/accessPolicy.ts` (`always_free`, `free_auth`, `telegram_channel`, `pro_only`, `pro_or_channel`, `pro_and_channel`) с безопасным fallback на `src/lib/config.ts`.
- Реализована таблица `ulpana_access_rules` и эндпоинт `/api/admin/access-rules` (GET/POST, защита через `verifyAdminRequest` и `isVipUser`).
- В `/admin` добавлена вкладка «Доступ к контенту» с переключателем режима (бета/боевой), пакетными кнопками (presets) и таблицей для всех 100 уроков с индивидуальными селекторами.
- Создано модальное окно `ChannelSubscribeModal` для учеников с возможностью мгновенной верификации и обновления профиля.
- Интегрировано в `CourseMap` и `page.tsx`.
- Прогон: 150/150 тестов (`npm test`), `typecheck` 0 ошибок, `npm run audit:intent` 0 нарушений.

**16.09.2026 — Защита разбора устной речи (P-10, R-15), Whisper и хранение аудио звонков:**
- Устранена проблема в `src/components/PhoneCallSimulator/usePhoneCall.ts`: пауза по таймауту тишины отправляла сырой текст Web Speech API на мобильных устройствах, минуя серверный Whisper (`/api/ai/transcribe`), из-за чего фонетическое искажение ("הכל" -> "עכל") попадало в разбор как орфографическая ошибка.
- В `/api/ai/phone/debrief` внедрено строгое правило: звонок является устной речью, запрещено критиковать орфографию, буквы, опечатки и омофоны (`ע/א/ה`, `כ/ק`, `ט/ת`, `ס/שׂ`). В `normalizeReport` добавлен фильтр, отсекающий любые галлюцинации LLM про буквы и правописание.
- В `DialogueReviewModal.tsx` разделены RTL иврит и LTR русский перевод (устранены инверсии скобок `(!...` в BiDi).
- В `usePhoneCall.ts` `currentMessages.forEach(async ...)` заменен на `await Promise.all(...)` перед вызовом `/api/calls/log`, что исключило сохранение временных `blob:` URL в базу данных. В Neon Postgres создана таблица `ulpana_audio_blobs` для хранения аудиозаписей звонков.
- В паспорте `docs/mechanics/stage-06-phone-call.md` зафиксирован инвариант P-10 (Debrief Oral Protection & Audio Persistence).

**16.09.2026 — Сохранение диалогов, просмотр сочинений в /admin и канонические URL диалогов (/dialogues/[id]):**
- Исправлено логирование сценарных диалогов (Этап 4, `ScriptedDialogueTrainer`): в `useScriptedDialogue.ts` подключено вычисление длительности, формирование транскрипта с аудио ученика, сохранение в `saveLocalCallLog` и отправка в БД через `/api/calls/log`.
- Проверена сохранность сочинений: эндпоинт `/api/essays/log` сохраняет работы в таблицу `ulpana_essays`. Создан защищенный роут `/api/admin/essays` с пагинацией и фильтрацией.
- В `/admin` добавлена вкладка «Сочинения» с фильтрами по оценкам, поиском по тексту/автору, плеером озвучки и раскрывающимся фидбеком от ИИ.
- По Варианту 2 реализованы канонические маршруты Next.js 16: `/dialogues/[id]` (динамическая страница с `generateMetadata` и OpenGraph) и `/dialogues` (каталог всех 100 диалогов с фильтрами уровней).
- Добавлена кнопка «Прямой URL» в `LessonView.tsx`, прямая ссылка «🎭 Диалоги» в `Navbar.tsx`, а также сохранение выбранного этапа урока в хеше URL (`#lesson-X/chat`, `#lesson-X/essay`, `#lesson-X/words`).
- Прогон: 150/150 тестов (`npm test`), `npm run typecheck` (0 ошибок), `npm run audit:intent` (зелёный), `npm run build` (успешная сборка Next.js 16).

**16.09.2026 — Защита от переспрашивания (Slot Memory P-03) и контекстный словарь Whisper STT:**
- Исправлена проблема распознавания Whisper («בְּדִירָה שְׁתַּיִם» расшифровывалось как «בג'רע שטיין» / «גרבטרשטיין»): в `usePhoneCall.ts` реализован генератор `buildPhoneRecognitionVocabulary` (числительные 1–10, ключевые слова сценария, варианты имени ученика), передаваемый в `transcribeAudioBlob` (расширен лимит словаря до 80 слов) и в `SpeechRecognition`. На реальных аудиозаписях из Neon DB точность распознавания Whisper достигла 100%.
- Реализован двухконтурный механизм Slot Memory (инвариант P-03, `src/lib/slotMemory.ts`):
  1. Серверный экстрактор `extractClosedSlots` определяет закрытые слоты (`name`, `wellbeing`, `apartment`, `coffee_sugar`, `coffee_size`, `coffee_milk`).
  2. Генератор жестких запретов `formatSlotMemoryPrompt` внедряет в системный промпт модели категорический запрет на повтор вопросов по закрытым слотам (исключен повторный вопрос «איך קוראים לך?»).
  3. Детерминированный предохранитель на выходе `filterRepeatedSlotQuestions` автоматически удаляет случайные утечки запрещенных вопросов из иврита, транскрипции и перевода.
- Добавлен `openai/gpt-oss-20b` в карусель моделей для телефонных звонков на Groq.
- Актуализирован паспорт механики `docs/mechanics/stage-06-phone-call.md`.
- Добавлены модульные тесты в `tests/phone-conversation-simulator.test.cjs`: 14/14 тестов телефонного тренажера успешно пройдены, общий люкс `npm test` — 152/152 passed, `npm run build` и `npm run audit:intent` зелёные.

**16.09.2026 — Сетевая устойчивость звонков (P-01) и очистка пререквизитов Уроков 1 и 3 (R-07):**
- Устранена проблема зависания звонка при медленном ответе/обрыве сети: в `usePhoneCall.ts` добавлен `AbortSignal.timeout(20000)`. При задержке интерфейс выводит сообщение о задержке сети и немедленно разблокирует микрофон для повтора. В паспорте `docs/mechanics/stage-06-phone-call.md` зафиксирован пункт 4 правила P-01.
- Исправлено методическое нарушение (R-07): в Уроке 1 из целей, подсказок и промпта убраны номер квартиры (`דירה 5`) и непройденный глагол `לָגוּר` (ученик отвечает строго изученным материалом: приветствие, самочувствие `הַכֹּל טוֹב` и имя `אֲנִי דָּוִד / שָׂרָה`). В Уроке 3 фраза `גר בדירה 4` заменена на тему городов урока: `גָּר בְּתֵל אָבִיב`.
- Актуализированы тесты `tests/pilot-mechanics-01-05.test.cjs`. 152/152 тестов (`npm test`), `npm run typecheck` и `npm run audit:intent` полностью зелёные.

**21.09.2026 — Ликвидация аппаратного ускорения x2 в видео и пересборка 57с эталона Урока 7 (R-25):**
- **Диагностика и устранение первопричины («чипманк-эффект» и спешка x2):**
  1. В `growth/scripts/generate_lesson_07_audio.mjs` вскрыта математическая ошибка в `convertMp3ToWav`: входной аудиопоток Google TTS имел частоту 24 000 Гц, а фильтр `asetrate` рассчитывался от 44 100 Гц (`Math.round(44100 * pitchFactor)`), что приводило к аппаратному разгону аудио в 1.75x–1.84x (до 7.5 слогов/сек) и писклявому тембру.
  2. Алгоритм полностью переписан: входной поток нормализуется, темп иврита выставлен на естественные спокойные `0.95x` (3.4–3.7 слогов/сек), закадровый голос диктора — на естественный `1.0x` (без искусственного зажима).
  3. В Акт 3 («Звонок с ИИ») добавлен полноценный двусторонний диалог с хозяином квартиры (Эли отвечает в трубке с аутентичным телефонным фильтром `highpass=300,lowpass=3400` $\rightarrow$ ученик спрашивает про мебель $\rightarrow$ Эли назначает просмотр на 18:00 $\rightarrow$ ученик подтверждает $\rightarrow$ звук завершения вызова). Исключен резкий обрыв звонка на полуслове.
- **Перезапись и сборка всех платформ:**
  1. Синхронизирован Playwright-скрипт `growth/scripts/record_lesson_07_video.mjs`: время на просмотр словаря, 2 секунды на фиксацию бейджа 98% «Отлично!», полный живой звонок.
  2. Итоговый хронометраж мастер-видео составил **57.02 секунды** (строго укладывается в 60-секундный лимит YouTube Shorts, Instagram Reels и TikTok).
  3. Все 6 вариантов (`youtube_shorts`, `instagram_reels`, `tiktok`, `telegram`, `facebook_reels`, `master`) пересобраны с промокодом на 3 дня и проверены покадрово.
  4. Проверки: `npm test` (251/251 passing, 100%), `npm run audit:intent` (100% Zero-Drift, 2535 карточек ComplexDrills).

**19.09.2026 — Маркетинг-Хаб, Реестр публикаций, Диагностика подключений и Партизанский радар лидов в /admin:**
- Создан диагностический монитор подключений `/api/admin/marketing/health` (Telegram Bot @Ulpinebot + права в канале @ulpana_il, Groq LLM, Gemini LLM, сессия WhatsApp Baileys, Meta) со светофором статусов и замером времени отклика (ms).
- Реализована система Deep Linking: маршруты `/lessons/[id]/call` (прямой запуск звонка с ИИ), `/lessons/[id]/stage/[stageId]` (прямой этап урока), `/decks/[id]` (прямой переход к колоде). Добавлена поддержка хэшей `#decks/` и `#deck-` в `src/app/page.tsx` с автоматическим сохранением промокода `?promo=CODE` в `localStorage` (`ulpana_pending_promo`).
- Создан Единый Реестр публикаций: хранилище `growth/data/publications.json`, CRUD API `/api/admin/marketing/publications` и интерфейс со ссылками на первоисточники (TikTok, YouTube, Telegram, Facebook, Instagram), форматами и генератором промо-ссылок.
- Создан Партизанский радар лидов (Lead Radar CRM): хранилище `growth/data/leads.json`, API `/api/admin/marketing/radar` и `/api/admin/marketing/radar/scan`, двухконтурный фильтр (RegExp + Groq/Gemini), генерация ответов с нужными ссылками, интерфейс в `/admin` с фильтрами и кнопками «Скопировать ответ» / «Открыть чат», автономный скрипт `growth/scripts/run_lead_radar.cjs`.
- Добавлен компонент `src/app/admin/components/AdminMarketingHub.tsx` и вкладка «Маркетинг & Контент» в `src/app/admin/page.tsx`.
- Тесты: 25/25 тестов пройдены (`tests/marketing-health-check.test.cjs`, `tests/deep-linking-routes.test.cjs`, `tests/marketing-registry-api.test.cjs`, `tests/lead-radar-filter.test.cjs`, `tests/decision-matrix-invariants.test.cjs`), аудит `npm run audit:intent` 100% зелёный.

**20.09.2026 — Пост-центричная таблица в Маркетинг-Хабе (Post-First Hub) и 1-клик публикация (коммит 75234b7):**
- **Пост-центричная модель публикации взамен «сырых файлов»:**
  - Сущность публикации расширена полями `campaignTitle`, `version`, `videoPath`, `caption` (в `growth/data/publications.json` и `src/app/api/admin/marketing/publications/route.ts`).
  - Каждая строка в таблице админки — законченный пост под ключ под конкретную соцсеть с адаптированным текстом (CTA в шапке для IG, в описании для YT, ссылка на бота для TG) и целевым промокодом (`YT`, `INSTA`, `TIKTOK`, `TG_CHANNEL`).
  - Добавлен пакет постов для **«Этап 5: Разговорные диалоги v2.0»** (YouTube Shorts, Telegram, Instagram Reels, TikTok).
- **Интерактивные возможности таблицы:**
  - Встроенный модальный HTML5-видеоплеер: кнопка ▶️ «Плеер» открывает просмотр ролика прямо в админке без перехода на сторонние страницы.
  - Кнопка 📋 «Скопировать текст» с визуальной индикацией для мгновенной вставки в соцсети.
  - Кнопка ⚡ «В 1 клик» для YouTube, Telegram и Facebook: атомарно отправляет материал через API боевого сервера и обновляет статус на `🟢 Вышел` с ссылкой на реальный пост/Shorts.
  - Фильтры по соцсетям, кампаниям и статусам публикаций.
- **Верификация:** `npx tsc --noEmit` (0 ошибок), `tests/decision-matrix-invariants.test.cjs` (13/13 pass), `npm run audit:intent` (100% Zero-Drift), `npm run build` (чистая сборка 44/44 роутов). Все изменения закоммичены и отправлены в `origin main` (коммит `75234b7`).

**20.09.2026 — Автономный Hetzner 24/7 демон, плоская таблица сообществ и честные статусы радара:**
- **Развертывание 24/7 демона на Hetzner VPS (`olatola.co.il` / `159.69.182.255`):** настроен процесс `ulpana-telegram-radar` в PM2 под управлением systemd (`pm2-root.service`), непрерывно слушающий сообщения по протоколу MTProto через gramjs в 5 супергруппах Telegram. Успешно проверена сквозная доставка алертов о болях основателю в Telegram-бот @ulpana_il.
- **Очистка данных и 100% достоверность (Zero Fake Data):** база `growth/data/leads.json` очищена до `[]` локально и на сервере Hetzner. Удалены все синтетические моки («Михаил», «Елена»). Кнопка тестовой генерации заменена на честную синхронизацию реальных данных `handleRefreshRadar`.
- **Плоская таблица сообществ взамен карточек:** в `AdminMarketingHub.tsx` внедрена плоская компактная таблица со всеми параметрами (Сеть, Сообщество / Ссылка, Категория болей, Аудитория, Статус автоматики, Эфир, Лидов, Действия).
- **Разделение сетей и честные статусы автоматизации:**
  - Добавлены табы фильтрации по соцсетям: `Все`, `✈️ Telegram`, `💬 WhatsApp`, `📘 Facebook`.
  - Telegram маркирован честным статусом `🟢 Hetzner 24/7 (Автоперехват)`.
  - WhatsApp и Facebook маркированы честным статусом `⚪ Ручной учёт (автомат off)` с явным уведомлением, что авто-перехват для них не настроен.
- **Верификация:** `npm run typecheck` (0 ошибок), `npm test` (все инварианты зелёные), `npm run build` (чистая сборка Next.js 16), `npm run audit:intent` (100% compliance). Выгружено в `main` и задеплоено на Vercel.

**17.09.2026 — Полная база предложений для режима «Комплекс» (R-18) с привязкой к урокам:**
- Для всех 125 глаголов тематических колод («Глаголы Алеф — Части 1, 2, 3, 4» и «Глаголы Бет») и всех 175 карточек 7 профессиональных колод выполнена привязка к урокам курса (1–100) взамен `lessonId: 0`.
- В `src/data/verbSentencesData.ts` создана и интегрирована база проверенных микро-предложений для 230 новых глаголов (100 глаголов курса + 130 уникальных базовых глаголов из профессиональных колод: Уход за пожилыми, Детский сад, Медицина, Бухгалтерия, Авторемонт, Библиотека, Автомойка). Суммарная база `VERB_SENTENCES_DATA` расширена с 25 до 255 глаголов (510 предложений).
- Все предложения строго подчиняются правилам R-04/R-06 (полный כתיב מלא с огласовками, исключён голый кубуц), V-02 (3–4 слова, максимум 5), V-04 (разметка по временам и предлогам), V-06 (академическая кириллическая транскрипция с 'h' и ударениями).
- Функция `getVerbSentenceGroup` поддерживает авторазрешение составных глагольных словосочетаний (по первому слову инфинитива).
- Все тесты пройдены: `tests/verb-sentences-matrix.test.cjs` (5/5), полный набор `npm test` (156/156 тестов зелёные), `npm run typecheck` (0 ошибок), `npm run audit:intent` (зелёный).

**17.09.2026 — Улучшение читаемости разбора ошибок (удаление line-through):**
- Заменено зачеркивание ивритских букв (`line-through`) в карточках разбора сочинений (`EssayEvaluationView.tsx`), телефонных звонков (`DialogueReviewModal.tsx`) и админ-панели (`admin/page.tsx`) на четкое выделение контрастными красными бейджами (`text-rose-600 dark:text-rose-400 font-bold`).
- Устранена проблема перекрытия верхних штрихов и огласовок в ивритских буквах при разборе орфографии.
- Все проверки пройдены: `npm test` (165/165 тестов зелёные), `npm run typecheck` (0 ошибок), `npm run build` (успешная сборка), `npm run audit:intent` (зелёный).

**17.09.2026 — Инверсия приоритета ИИ-моделей: Gemini как основной движок + Groq как страховка (R-21):**
- По прямому архитектурному требованию пользователя Gemini с ключом из Google AI Studio (`GEMINI_PRIMARY_API_KEY`) установлен основным движком для всех задач проекта. Модели Groq (`qwen3.8-27b`, `gpt-oss-120b`, `gpt-oss-20b`) переведены в режим автоматической отказоустойчивой страховки (fallback insurance).
- Инвертирован порядок опроса во всех 7 серверных AI-роутах (`dialogue/evaluate`, `phone`, `phone/debrief`, `chat`, `essay/evaluate`, `lookup`, `conjugate`):
  1. Первым опрашивается Gemini (`gemini-3.5-flash-lite` с `thinkingBudget: 512` или `gemini-3.6-flash`).
  2. При сбое, таймауте, ошибке 4xx/5xx или исчерпании квоты Gemini управление прозрачно переходит к карусели моделей Groq как страховке.
  3. Если оба внешних провайдера недоступны — срабатывают безопасные локальные эвристики и автоответчики.
- В `src/lib/storage.ts` значение `aiProvider` по умолчанию в `DEFAULT_PROFILE` изменено на `'gemini'`.
- В `DECISION_MATRIX.md` добавлено правило R-21 (Блок 8) и зафиксирован запрет на опрос Groq вперед Gemini в Superseded Log.
- Актуализирован паспорт механики звонков `docs/mechanics/stage-06-phone-call.md` (P-07, P-10).
- В `tests/ai-provider-routing.test.cjs` добавлены сквозные тесты: подтвержден вызов Gemini первым и перехват управления моделями Groq при сбое Gemini.
**17.09.2026 — Масштабирование режима «Комплекс» (ComplexDrills R-18) для всех частей речи (Пакет 1: Уроки 11–25):**
- Устранена первопричина спама всплывающими окнами безопасности IDE при фоновой генерации: создан изолированный профиль субагентов `drill_generator` (`enable_write_tools: false`, без доступа к системным командам), работающий строго в памяти и исключающий блокировки среды.
- В 4 параллельных потоках субагентов сгенерирован и валидирован полный комплект учебных микро-предложений для 136 слов всех частей речи (Уроки 11–25: 92 существительных, 9 прилагательных, 35 устойчивых выражений/наречий/предлогов).
- Все фразы подчиняются правилам R-04/R-06 (полное כתיב מלא с огласовками, исключён голый кубуц), R-18 (3–4 слова, лексика курса), R-09 (академическая Pealim-транскрипция с 'h' и знаками ударения).
- База существительных `NOUN_DRILLS_DATA` расширена с 95 до 222 элементов.
- База прилагательных `ADJECTIVE_DRILLS_DATA` расширена с 12 до 21 элемента с полными сетками из 4 форм (ms/fs/mp/fp).
- Общий объём покрытия режима «Комплекс» достиг 273 слов (248 неглагольных слов + 25 глаголов Алеф 1).
- Полная проверка: 176/176 тестов (`npm test`, 0 failures), `tests/complex-drills-invariants.test.cjs` (4/4 passed), `npm run typecheck` (0 ошибок), `npm run audit:intent` (100% зелёный, 0 дрейфа).

**17.09.2026 — Масштабирование режима «Комплекс» (Пакет 2: Уроки 26–50):**
- В 4 параллельных потоках субагентов сгенерирован и валидирован комплект учебных микро-предложений для лексики уроков 26–50 (гостиница, аптека, транспорт, кулинария, школа, природа, бюрократия, коммуникация, учёба).
- База существительных `NOUN_DRILLS_DATA` расширена с 222 до 329 элементов (+107 существительных/выражений).
- База прилагательных `ADJECTIVE_DRILLS_DATA` расширена с 21 до 38 элементов (+17 прилагательных).
- Обновлён инвариант теста `complex-drills-invariants.test.cjs`: верхняя граница `minLesson` расширена с 25 до 50.
- Коммит: `00cf3e8`. Полная проверка: 176/176 тестов, 4/4 инварианты, 0 ошибок TypeScript, audit 100%.


**16.09.2026 — Устранение зависания завершения звонка во 2-м уроке (P-05), карусель AI и разблокировка красной трубки:**
- Диагностирована и устранена причина, по которой во 2-м уроке (кафе «Арома») не завершался звонок («вот уже минуту не положена трубка»):
  1. В `src/app/api/ai/phone/route.ts` флаг завершения `isDone` безусловно приравнивался к `shouldForceFinalTurn` (`userTurnsCount >= 3`). Когда бариста во 2-м раунде после озвучивания цены прощался («תּוֹדָה רַבָּה, יוֹם טוֹב!») и модель возвращала `shouldHangUp: true`, сервер затирал это значение на `false`. В результате приложение держало линию открытой, а ученик ждал сброса.
  2. `isDone` теперь вычисляется с учетом `parsed.shouldHangUp === true` от модели и маркеров прощания ИИ (`יוֹם טוֹב`, `לְהִתְרָאוֹת`, `בַּיי`, `יוֹם נִפְלָא`), а также фраз благодарности ученика (`תּוֹדָה רַבָּה`, `יוֹם טוֹב`).
  3. В `src/app/api/ai/phone/route.ts` устранено методическое искажение: системная инструкция первого урока про знакомство и номер квартиры (`דירה 4`) ошибочно применялась к урокам `<= 3`. Теперь она строго изолирована условием `lessonNumber === 1`.
  4. В `src/data/phoneScenarios.ts` к Уроку 2 добавлена 4-я подсказка для теплого завершения звонка (`תּוֹדָה רַבָּה, יוֹם טוֹב!`), а также адаптация `initialGreeting` для женского рода (`מָה אַתְּ רוֹצָה`).
  5. В `src/components/PhoneCallSimulator/ActiveCallView.tsx` убран атрибут `disabled={isAiHangingUp}` с красной трубки («Положить трубку») — теперь пользователь может сбросить вызов мгновенно в любой момент.
  6. В `src/lib/aiModels.ts` для звонков отключен сбойный фолбэк `openai/gpt-oss-20b` (падавший на Groq с 400 по JSON validation), обеспечив мгновенный прозрачный переход на сверхбыстрый Gemini `gemini-3.5-flash-lite`.
  7. Актуализирован паспорт `docs/mechanics/stage-06-phone-call.md`, добавлен модульный тест в `tests/phone-conversation-simulator.test.cjs`. Все 153 теста (`npm test`), `npm run typecheck`, `npm run audit:intent` полностью зелёные.

**16.09.2026 — Ликвидация ложных срабатываний и зависания звонка (P-01, R-15, R-19):**
- Устранены корневые причины нестабильности звонка во 2-м уроке («иногда сразу думает», «иногда зависает», дедлок микрофона):
  1. В `src/lib/speech.ts` повышен порог детекции речи VAD (минимум 4 фрейма ~200мс или пик громкости `avg > threshold + 8`), минимальный размер аудиочанка для Whisper V3 поднят до 1500 байт (~0.3с речи), очищаются буферы шумов и галлюцинации тишины.
  2. В `src/components/PhoneCallSimulator/usePhoneCall.ts` упразднен конкурирующий таймер `silenceTimeoutRef` из `onResult` — детекция пауз в речи полностью передана Web Audio VAD в `speech.ts` (Single-Source VAD).
  3. `handleSendMessage` строго обернут в `try ... finally`, гарантируя сброс `isSendingRef.current = false` и `setAiLoading(false)` при любых сетевых абортах, задержках или смене поколения звонка.
  4. Синхронизирован паспорт `docs/mechanics/stage-06-phone-call.md` (P-01) и `DECISION_MATRIX.md` (обновлен до v1.4.1, в Superseded Log зафиксирован запрет конкурирующих таймеров тишины и ранних выходов без сброса флагов).
  5. Все проверки пройдены: 153/153 тестов (`npm test`), `npm run typecheck` (0 ошибок), `npm run audit:intent` (100% green, 0 дрейфа) и `npm run build` (38/38 маршрутов).

**17.09.2026 — Полное покрытие всех 100 уроков курса в режиме «Комплекс» (ComplexDrills, R-18):**
- Тренажёр «Комплекс» (`ComplexDrills`, инвариант R-18, паспорт `docs/mechanics/verb-sentences-matrix.md`) полностью масштабирован на весь курс «Ульпан Алеф» (уроки 1–100: базовый курс Алеф 1–50 и продвинутый курс Бет 51–100):
  1. `src/data/drills/nounDrillsData.ts`: расширен до **766** уникальных словарных записей (существительные, устойчивые выражения, наречия). Каждая запись содержит единственное и множественное число, грамматический род, детерминированную учебную фразу на иврите из 3–4 слов в полном написании (כתיב מלא с огласовками, R-04, R-06), академическую транскрипцию Pealim и русский перевод.
  2. `src/data/drills/adjectiveDrillsData.ts`: расширен до **49** уникальных записей прилагательных с полной матрицей 4 форм (ms, fs, mp, fp), транскрипциями, переводами и фразами согласования.
  3. `tests/complex-drills-invariants.test.cjs`: обновлены пороги проверки целостности (не менее 750 существительных и 45 прилагательных, валидация длины предложений 2–6 слов, проверка отсутствия архаичных кубуцев без вав, диапазон уроков `minLesson <= 100`).
  4. Проверки: `npm run typecheck` (0 ошибок), `node --require ./tests/register.cjs --test tests/complex-drills-invariants.test.cjs` (4/4 passed), `npm test` (176/176 passing), `npm run audit:intent` (100% зелёный статус соблюдения матрицы).

**17.09.2026 — Ликвидация галлюцинаций Whisper при тишине и внедрение Audio Energy Gate (P-01, R-15, R-19):**
- Диагностирована первопричина появления ложных фраз («דירה, דירות כאלה שיש לסלון, מה פעמים?») при намеренном молчании ученика в телефонном звонке:
  1. Фоновый комнатный шум (-45..-51 dB) ложно активировал VAD из-за низкого порога `speechThreshold = 10`.
  2. Записанная тишина вместе со словарными подсказками сценария Урока 7 передавалась в Groq Whisper.
  3. Декодер Whisper синтезировал фразы из переданного `prompt` (Prompt Conditioning Hallucination) с ложной уверенностью `no_speech_prob < 0.01`.
- Внедрено многоуровневое архитектурное решение:
  1. В `src/lib/speech.ts` реализован Time-Domain RMS Audio Energy Gate (`peakRmsDbInCurrentChunk > -35 dBFS`, `peakAvgInCurrentChunk >= 15`) и адаптивное отслеживание фонового шума тишины (`ambientNoiseFloor`). Фоновые шумы и тишина отбрасываются до отправки на сервер.
  2. В `src/components/PhoneCallSimulator/usePhoneCall.ts` порог детекции голоса `speechThreshold` поднят до 16.
  3. В `src/app/api/ai/transcribe/route.ts` и `src/lib/speechTranscription.ts` внедрён серверный детектор `isWhisperPromptHallucination` и порог отсечки Whisper `avg_logprob < -1.0`, а также фильтрация коротких фантомов тишины («תודה רבה», «שלום»).
  4. В `tests/whisper-prompt-resilience.test.cjs` добавлены регрессионные тесты детекции реальных галлюцинаций из боевой базы и пропуска реальной речи.
- Синхронизированы `DECISION_MATRIX.md` (v1.4.3, Superseded Log) и паспорт `docs/mechanics/stage-06-phone-call.md` (P-01.10).
- Проверки: `npm run typecheck` (0 ошибок), `npm test` (176/176 passing), `npm run audit:intent` (100% зелёный), `npm run build` (Next.js 16, 38/38 routes скомпилированы).

**18.09.2026 — Синхронизация блока семьи корня в режиме «Комплекс» со стандартом обычного режима:**
- В `src/components/FlashcardTrainer/modes/ComplexDrillMode.tsx` блок «Семья корня» приведен к поведению обычного режима: ивритское слово (с учетом `showNikkud` и `fontStyle`), кириллическая Pealim-транскрипция (`showTranscription`), русский перевод с бейджем части речи, отдельная кнопка озвучки (`<Volume2 />`, `speechRate` из профиля, R-17), кликабельная карточка с открытием модального окна разбора (`WordLookupModal`, R-20) и индикатор сохранения в личном словаре (`Check`).
- Синхронизирован паспорт `docs/mechanics/verb-sentences-matrix.md` (V-09).
- Проверки: `npx tsc --noEmit` (0 ошибок), `npm test` (179/179 passed), `npm run audit:intent` (100% зелёный, 2535/2535 слов покрыты в ComplexDrills).
- Все изменения закоммичены и отправлены в GitHub `origin/main` (`4d6d4c6659c93627077b79cdceea5d9017286e43`).

**18.09.2026 — Фонетическая нормализация ударения «сабáба» на мобильных движках (iOS Carmit / Android) и инвалидация PWA-кэша:**
- Устранена причина, по которой на телефонах слово «סבבה» продолжало звучать с окситонным ударением («сабабА»), несмотря на корректность на десктопе:
  1. Мобильные синтезаторы (Apple Carmit на iOS, Google Speech на Android) интерпретируют любое изолированное слово с окончанием на букву ה (даже с патахом под ב) как существительное женского рода с ударением на последний слог.
  2. В `src/lib/speech.ts` в `cleanHebrewForSpeech` фонетическое представление для сленгового слова «сабаба» переведено на дефисную форму `סַבָּ-בָּה`. Дефис разделяет токен на основу `סַבָּ` (ударение на последний слог основы, т.е. второй слог всего слова) и энклитический безударный суффикс `-בָּה`. Акустический анализ RMS и транскрипция Whisper подтвердили естественное ударение «са-БА-ба» с пиковой мощностью на втором слоге (4330 RMS vs 1883 на третьем слоге).
  3. В `public/sw.js` обновлена версия ServiceWorker кэша (`CACHE_NAME = 'ulpana-hebrew-v4'`), что форсирует инвалидацию кэшированного JS-бандла на мобильных PWA-устройствах.
  4. Обновлены юнит-тесты в `tests/transcription-preserve-vav.test.cjs`.
- Проверки: 186/186 тестов (`npm test`, 0 failures), `npm run typecheck` (0 ошибок), `npm run audit:intent` (100% зелёный), `npm run build` (38/38 routes успешно скомпилированы). Изменения отправлены в GitHub `origin/main` (`2bc8bf3`).

**18.09.2026 — Регистрация официального YouTube-канала Ульпана и пресет промокода YT:**
- В `growth/CHANNELS_AND_ASSETS.md` зафиксирован новый официальный YouTube-канал Ульпана: ID `UC1kWxNhUydNncIRzTbBzWJw`, публичный URL `https://www.youtube.com/channel/UC1kWxNhUydNncIRzTbBzWJw`, Studio URL `https://studio.youtube.com/channel/UC1kWxNhUydNncIRzTbBzWJw`.
- В `src/app/admin/page.tsx` добавлен пресет `YT` (30 дней PRO, 500 активаций, описание видео, Shorts и закрепленный комментарий) в `CHANNEL_PRESETS` для автоматической генерации ссылок вида `https://ulpana-hebrew.vercel.app/?promo=YT` в 1 клик.
- Проверки: `tests/promo-capture-and-management.test.cjs` (5/5 pass), `npm test` (186/186 pass), `npm run audit:intent` (100% зелёный).

**18.09.2026 — Внедрение экспресс-диагностики аудио и TTS в шапку (Header JSON Exporter):**
- Для выявления причин специфичного поведения мобильных движков речи прямо на реальных устройствах внедрен 1-клик инструмент сбора телеметрии:
  1. Создан модуль `src/lib/deviceDiagnostics.ts`: собирает UserAgent, платформу, PWA Service Worker статус, кэш `caches.keys()`, полный список голосов Web Speech API (`speechSynthesis.getVoices()`), фильтрованные ивритские голоса, результат прогона `cleanHebrewForSpeech('סַבָּבָה')` и живой тест произношения со временем отклика.
  2. Создано модальное окно `src/components/DeviceDiagnosticsModal.tsx`: визуализирует ключевые параметры, предоставляет 1-клик кнопки «Скопировать JSON в буфер» и «Скачать файл», а также повторное воспроизведение слова.
  3. В `src/components/Navbar.tsx` в шапку добавлена компактная кнопка с иконкой пульса (`Activity`). По нажатию автоматически запускается тест, скачивается файл `ulpana-speech-diag-*.json`, данные копируются в буфер обмена и открывается модальное окно. Поддерживается автозапуск по ссылке `?diag=1`.
  4. Добавлены тесты `tests/device-diagnostics.test.cjs` (3/3 pass).
- Проверки: 189/189 тестов (`npm test`, 0 failures), `npm run typecheck` (0 ошибок), `npm run audit:intent` (100% зелёный), `npm run build` (38/38 routes скомпилированы).

**18.09.2026 — Устранение дублирования слова на иврите в режиме «Комплекс» (ComplexDrills, R-18):**
- Устранена причина повторного воспроизведения слова на иврите при подтверждении карточки в слуховом тренажёре:
  1. В `src/components/FlashcardTrainer/modes/ComplexDrillMode.tsx` в методе `runConfirmationSequence` для существительных выполнялась озвучка конкатенированной строки `${singularHe} — ${pluralHe}`. Для 540 неисчисляемых существительных, названий стран и языков (`pluralHe === singularHe`) синтезатор произносил одно и то же слово дважды («קפה קפה»), а для исчисляемых читал обе формы слитно без паузы при наличии только русского перевода единственного числа.
  2. В `runConfirmationSequence` озвучка слова на иврите приведена к единому детерминированному стандарту R-18: строго одно разбираемое слово на иврите (`wordHe = targetWordVocalized || singularHe`), после чего звучит русский перевод и грамматический род.
  3. В UI открытой карточки (`revealed`) добавлена проверка `hasDistinctPlural`: для неисчисляемых слов выводится аккуратная одиночная карточка («Словарная форма»), исключая дублирование одинаковых колонок единственного и множественного числа. Для исчисляемых слов отображаются обе колонки с раздельной озвучкой при клике и кнопка воспроизведения пары через естественную акустическую паузу (запятую).
  4. В кликах по словам предложений (`handleTokenClick`) и семьи корня (`handleRootFamilyWordClick`) обеспечен гарантированный сброс таймера паузы (`clearInterval(timerRef.current)`), `stopSpeech()` и инкремент `playCycleIdRef.current`, предотвращая наложение фонового звука при открытии модального окна разбора (`WordLookupModal`, R-20).
  5. В `src/lib/speech.ts` добавлена фонетическая нормализация патаха для слова «חתונה» (`חַתּוּנָה` / `חַתּוּנוֹת`), исключающая искажение произношения в синтезаторах речи.
  6. Обновлены `DECISION_MATRIX.md` (v1.4.5, R-18, Superseded Log), паспорт `docs/mechanics/verb-sentences-matrix.md` (V-07) и добавлен регрессионный тест `tests/complex-drills-invariants.test.cjs`.
- Проверки: `npm test` (190/190 passing, 0 failures), `npx tsc --noEmit` (0 ошибок), `npm run audit:intent` (100% зелёный, 2535/2535 слов).

**19.09.2026 — Регистрация официального TikTok-аккаунта (@ulpanaalef) и пресет промокода TIKTOK:**
- В `growth/CHANNELS_AND_ASSETS.md`, `growth/FOUNDER_TODO.md` и `growth/MARKETING_STRATEGY.md` зафиксирован официальный TikTok-профиль проекта: [`https://www.tiktok.com/@ulpanaalef`](https://www.tiktok.com/@ulpanaalef) (`@ulpanaalef`), вход через Google (`azr2001.g@gmail.com`). Назначение: публикация вертикальных видео (9:16) без водяных знаков для органического вирального охвата.
- В `src/app/admin/page.tsx` добавлен пресет `TIKTOK` (14 дней PRO, 500 активаций, ссылка в описании био / видео) в `CHANNEL_PRESETS` для автоматической генерации ссылок вида `https://ulpana-hebrew.vercel.app/?promo=TIKTOK` в 1 клик.
- Обновлен тест `tests/promo-capture-and-management.test.cjs` с проверкой нового пресета.
- В `growth/FOUNDER_TODO.md` и `growth/CHANNELS_AND_ASSETS.md` добавлен бэклог-план партизанского мониторинга израильских WhatsApp-сообществ (WhatsApp Lead Radar на базе Baileys WebSocket API) со связкой с Telegram-алертами фаундеру в режиме Listen-Only.
- Проверки: `node --test tests/promo-capture-and-management.test.cjs tests/decision-matrix-invariants.test.cjs` (17/17 pass), `npm run audit:intent` (100% зелёный, 0 нарушений).

**19.09.2026 — Регламент вирального видеопроизводства (R-25, VIDEO_PRODUCTION_PLAYBOOK.md) и запуск первого ролика:**
- Разработан и опубликован первый виральный ролик «Duolingo vs Реальность: курьер Wolt» в Telegram ([t.me/ulpana_il/9](https://t.me/ulpana_il/9), 29с, 9:16 vertical).
- В `DECISION_MATRIX.md` (v1.4.8, Блок 9) утверждено постоянное правило **R-25 (Стандарты вирального видеопроизводства)** и расширен `Superseded Log`:
  1. *Абсолютное позиционирование сцен:* запрещен `position: relative` на корневых блоках сцен внутри мобильного контейнера (выталкивал сцены 2 и 3 за пределы экрана); все сцены строго `position: absolute; inset: 0; visibility: hidden;` с переключением через `.active`.
  2. *Акустический монтаж и de-clicking:* запрещена грубая нарезка на стыках взрывных согласных (паразитный звук «к» от «כבר»); внедрен RMS-анализ границы тишины, косинусное сглаживание 15–30ms и прямой синтез пауз/запинок через TTS. Русский закадр ускорен до `atempo=1.22`, ступор ученика замедлен до `0.72..0.80x`.
  3. *Плотность экрана:* запрещены `margin: auto 0` и пустые зоны; контекст задачи и Live HUD видны с первой миллисекунды активации сцены.
  4. *Пиксельные клики:* координаты `showTouch(x, y)` рассчитываются строго по центрам `boundingBox()` элементов.
  5. *Юридический комплаенс:* сравнительная реклама в Израиле законна (ст. 19 Закона об авторском праве); для платного таргета зафиксировано правило замены прямых товарных знаков на сатирические тропы («с зеленой совой»).
- Создан подробный паспорт-руководство: [`growth/VIDEO_PRODUCTION_PLAYBOOK.md`](file:///c:/Users/azrie/Documents/antigravity/goofy-maxwell/growth/VIDEO_PRODUCTION_PLAYBOOK.md) с чек-листом проверки перед выгрузкой.
- Проверки: `npm run audit:intent` (100% зелёный, 2535/2535 ComplexDrills).

**19.09.2026 — Видеопроизводство: пилот Этапа 5 (v1.0), аудит ошибок и спецификация How-To скринкаста (v2.0):**
1. **Пилот v1.0 (завершён и выгружен):**
   - Собран и опубликован в Telegram (@ulpana_il, Message ID: 10) пилотный ролик для Этапа 5 («Диалоги»): `public/demo/tutorials/stage_05_dialogue_v1.mp4` (27с, 1.50 MB, 9:16 vertical). Зафиксирован в `growth/tutorials/registry.json`.
2. **Аудит ошибок пилота v1.0 (обратная связь пользователя):**
   - ❌ *Инженерный жаргон:* Недопустимо вываливать пользователю названия бэкенд-библиотек («серверный Whisper» и т.п.). Говорить только на языке пользовательской ценности («ИИ слушает без спешки», «оценка каждого слова»).
   - ❌ *Одинаковый голос:* Бариста и ученик зазвучали одинаковым синтезатором, диалог превратился в монолог. Необходим строгий контрастный кастинг (мужской бариста vs женский/контрастный ученик).
   - ❌ *Реклама вместо реальной механики:* Создан искусственный HTML-мок с огромными плашками вместо записи настоящего пошагового интерфейса сайта.
3. **Спецификация для следующего шага (How-To Скринкаст Этапа 5 v2.0):**
   - **Экран:** Реальный роут приложения `http://localhost:3000/lesson/2?stage=5` (мобильный вьюпорт 390x844 @2x, Playwright).
   - **Пошаговый сценарий в кадре:**
     - Шаг 1: Экран этапа 5 на сайте, клик тач-курсора по карточке выбора роли (Клиент).
     - Шаг 2: Клик по реальной кнопке «Начать».
     - Шаг 3: Появление реального бабла баристы, воспроизведение реплики (`«שָׁלוֹם! מָה אַתָּה רוֹצֶה לִשְׁתּוֹת?»`, мужской голос).
     - Шаг 4: Зажатие реальной кнопки микрофона тач-курсором, воспроизведение ответа ученика (`«אֲנִי רוֹצֶה קָפֶה קָטָן, בְּבַקָּשָׁה»`, контрастный голос).
     - Шаг 5: Отпускание микрофона, появление реального бейджа оценки (98%) и пословной подсветки в реальном UI.
     - Шаг 6: Клик по реальной кнопке перехода к следующему этапу (Звонок).
   - **Закадровый голос диктора:** Чёткие инструкции по шагам (без упоминания «Whisper», «моделей» и «серверов»).

**19.09.2026 — Видеопроизводство: создание обучающего How-To скринкаста Этапа 5 (v2.0, R-25):**
- **Устранены все замечания к звуку и темпу:**
  1. *Ликвидировано искусственное ускорение:* Полностью удалено ускорение закадровой русской речи `1.22x` (было артефактом промо-Reels). Речь диктора синтезирована в естественном учебном темпе `1.0x` (`ru-RU-DmitryNeural`).
  2. *Спокойный темп иврита (R-17):* Реплики диалога синтезированы через нейросеть Microsoft Edge (`he-IL-AvriNeural` для баристы и `he-IL-HilaNeural` для ученика) с замедлением `rate: '-20%'` (`0.8x`), обеспечивающим чёткую дикцию для начинающих.
  3. *Устранение белого экрана и рассинхрона:* Внедрен предварительный прогрев роута Playwright до старта контекста записи, рассчитано точное смещение предзагрузки (`uiOffsetSec = 7.13s`) с автоматической обрезкой через FFmpeg `filter_complex`. Ролик начинается с кадра 0.0s сразу на живом интерфейсе `ListeningView` (0 секунд ожидания, 0 секунд лоадера).
  4. *Интеграция с реальным DOM:* Все взаимодействия (выбор роли, зажатие микрофона, отправка фразы, получение оценки 98%, переход к финалу) выполняются тач-кликами по реальным селекторам сайта `http://localhost:3000/#lesson-2/chat`.
  5. *Характеристики готового видео:* `public/demo/tutorials/stage_05_dialogue_v2.mp4` (хронометраж 34с, 1.21 MB, вертикальный формат 9:16, 390x844 @2x). Контрольные кадры зафиксированы в `public/demo/tutorials/verification_frames/` (`frame_0.5s.jpg`, `frame_11.5s.jpg`, `frame_20.0s.jpg`).
  6. *Аудит соблюдения матрицы:* `npm run audit:intent` пройден на 100%, 0 дрейфа, все инварианты зеленые.

**21.09.2026 — Двухголосый студийный нейросетевой синтез (Microsoft Edge Neural TTS) и динамическая гендерная адаптация предложений (R-17, R-18):**
- Устранена системная проблема озвучки предложений: ранее при воспроизведении женский голос Google TTS без огласовок читал фразу «אֲנִי רוֹצֶה מַיִם קָרִים» (мужской текст, «роце») как омограф «ани роца», что нарушало методическую согласованность и языковую интуицию ученика.
- Реализована двухголосая архитектура на базе нейросетей Microsoft Edge Neural TTS:
  1. ♂ `he-IL-AvriNeural`: глубокий чистый мужской голос для мужских профилей и базовых/мужских предложений.
  2. ♀ `he-IL-HilaNeural`: мягкий чистый женский голос для женских профилей и женских вариантов предложений.
  3. Полные огласовки (никуд) сохранены при передаче в синтезатор, гарантируя 100% точность ударений и полное отсутствие омографических искажений.
- Разработан модуль `src/lib/drills/sentenceGenderAdapter.ts`:
  - 3241 пар глаголов настоящего времени из Pealim SSOT (`VERB_CONJUGATIONS_DATABASE`) для точной морфологической адаптации.
  - Академическая расстановка ударений Pealim для женских форм: сеголатные глаголы (-et/-at: `охéлет`, `йода́ат`) с пенультимативным ударением, формы на -a (`роца́`, `гара́`) — с окситонным.
  - Безопасная контекстная замена кириллических токенов с акцентированными гласными.
  - Динамическая адаптация 1-го и 2-го лица под `userProfile.gender === 'female'`.
- Пакетная генерация и актуализация каталога:
  - Сгенерированы все 213 женских аудиофайлов (`s_*_f.mp3`) голосом `he-IL-HilaNeural`.
  - Сгенерированы все 213 мужских аудиофайлов (`s_*.mp3`) голосом `he-IL-AvriNeural`.
  - Обновлен манифест `public/audio/sentences/manifest.json`: базовые ключи ведут на мужские файлы, суффиксные ключи `${key}::female` — на женские файлы.
- Интеграция в клиентский интерфейс и студию:
  - `src/components/FlashcardTrainer/modes/ComplexDrillMode.tsx` и `ComplexVerbMode.tsx`: динамическая адаптация текста, огласовок и транскрипции при `userProfile.gender === 'female'`, передача пола в `speakHebrew({ gender })`.
  - `src/lib/speech.ts`: поддержка выбора движка `'edge_neural' | 'google_cloud' | 'current'`, чтение `gender` из профиля, автоматическое направление на нужный аудиофайл из манифеста.
  - `src/app/admin/components/AdminAudioSentencesTab.tsx` и `/api/admin/audio-sentences`: 3-позиционный переключатель движка, фильтр М/Ж вариантов, бейджи рода, индивидуальные кнопки предпрослушивания и раздельная генерация полов.
- Проверки: `tests/gender-sentences-and-edge-audio.test.cjs` (5/5 pass), `tests/decision-matrix-invariants.test.cjs` (13/13 pass), `npm test` (245/245 pass, 0 failures), `npm run typecheck` (0 ошибок), `npm run audit:intent` (100% зелёный, 2535/2535 ComplexDrills).

Историческая сверка 23 замечаний завершена: 11 подтверждено, 6 частично, 4 предложения, 1 опровергнуто, 1 недостаточно оснований. Ревью pilot-01-05-review.md и pilot-remediation.json сохраняют исходные вердикты отдельно от состояния исправлений; не начинать сверку заново.

### Текущий кандидат и единственный следующий шаг разработчика

**53830cbc0bf72fad7b6dea145da519ee24344e22 ещё НЕ ПРИНЯТ.** Ветка fix/pilot-manual-language-consistency, отдельная рабочая копия `C:\Users\azrie\Documents\antigravity\goofy-maxwell-manual-language`, чистая при проверке 06:17. Задача pilot-manual-language-consistency.md была доставлена Gemini в 05:11; исходное поручение не повторять.

Языковые правки у-млафэфонӣм, самостоятельного תּוֹדָה и нормативного настоящего гарá проверены. Независимые три проверки реального buildInitialMessage и DOM упражнений проходят; те же проверки со старыми модулями дают три ожидаемых падения, только со старым helpers — одно. Полное сравнение сохранило урок 4, все 95 записей уроков 6–100, 200 телефонов, 100 сочинений и 100 сценарных диалогов. Доказательства: evidence/manual-language-5383-integrity.json и manual-language-5383-consumer-checks.json.

**Нужно доставить только results/pilot-manual-language-5383-review.md. Оно НЕ доставлено.** Тест 3 Gemini копирует сравнение вместо вызова компонента; тест 5 читает старые строки вместо отрицательного прогона; тест 6 выдаёт существование данных за полное сравнение. Сохранить корректные исходные правки, доработать тесты и отчёт в той же ветке. В 06:34 архитектор независимо подтвердил 97/97, типы, сборку 33/33, audit 0 vulnerabilities и нулевой lint трёх критических компонентов плюс двух изменённых исходников и нового теста. Это не закрывает недостатки трёх тестов. Доказательство evidence/manual-language-5383-full-checks.json. Package/lock не менялись, npm ci повторно не выполнялась; DB-интеграция, полный src lint, живой ИИ и звук в этом прогоне не проверялись. После доработки повторять проверки по изменённой области; ранее проверенную сборку можно учитывать лишь после подтверждения неизменности приложения/конфигурации/зависимостей.

Канал: Antigravity → проект goofy-maxwell → «Аудит Качества Уроков Иврита», Gemini 3.8 Flash High, Computer Use @oai/sky. В 05:39 две попытки активации вернули `failed to activate captured window`. В 06:17 свежий снимок показывал только фон Windows, тогда как дерево доступности оставалось от Antigravity; по старым индексам/координатам не действовать. Это не подтверждает конкретную причину или блокировку Windows. Новых сообщений/коммитов Gemini нет. Не считать сохранение ревью фактической отправкой. Детали канала — project-control/gemini-channel.md.

### Очередь после приёмки кандидата

1. tasks/chat-pilot-gender-suggestions.md — женские подсказки обычного ИИ-диалога, реальные начальные сообщения, prompts и fallback. Подготовлено, НЕ отправлено; назначить новую базу/ветку/копию после приёмки 5383 или его исправленной версии.
2. Черновик ex3-7/ex4-7 — `work/exercises-03-04-task-draft.md` в рабочей папке архитектора. НЕ отправлен и не добавлен как отдельная задача панели; сначала ручные данные и chat-pilot-gender.
3. tasks/pilot-audit-v2.md, версия 0.2 — карта целей и всех механик, отдельные статусы языка/методики/поведения и правила доказательств. Процедура подготовлена, повторный аудит НЕ отправлен и НЕ проведён. evidence/pilot-v2-inventory-b2dd.json фиксирует объём, не качество.
4. tasks/quality-gates-implementation.md — подготовлено, НЕ отправлено. Локальный кандидат CI до отдельного разрешения удалённых изменений.

### Подтверждённые препятствия выпуску

- **Telegram:** последний независимый POST создания сессии — 503. Vercel CLI Logged out, браузер Login. Точные Production-конфигурация и версия не прочитаны; полный вход не принят. Не повторять генерацию webhook secret и небезопасный откат. Отдельная попытка Chrome остановлена защитой проверки URL — не обходить и не повторять одинаковые попытки. Ревью telegram-login-review.md; прежние результаты не выдавать за свежий запуск.
- **GitHub:** на 06:03 удалённая main ec4d663, локальная main впереди на 13 коммитов. API: protected=false, checks off, действующих rules нет, workflow=0, runs=0. Endpoint полной protection вернул 403, причина не установлена. Локальная .github/workflows/beta-checks.yml не отслеживается и не изменялась. Ревью quality-gates-review.md; evidence/github-gates-readonly.json. Версия сайта из Git SHA не выводится.
- **Credential:** действующий GitHub PAT всё ещё находится в remote URL и ошибочно попал в диагностический вывод архитектора. Значение не записано в отчёты. Требуется штатная замена/отзыв и безопасное хранение; это ещё не выполнено. Не выводить git remote -v, raw config или env. Старый Groq-ключ из истории повторно не проверялся.
- **Коммерческая готовность:** состав выпуска, полная приёмка контента/живого ИИ/звука, ученическая бета, хранение аудио и восстановление БД не подтверждены. project-control/beta-acceptance.md задаёт критерии; общий процент готовности не рассчитывается.

### Сохранность работы

Основной репозиторий содержит незакоммиченные AGENTS.md, DEVELOPMENT.md, RELEASE_READINESS.md, .github/ и project-control/. Сохранять их, не смешивать с кодом Gemini. Прежние review-серверы остановлены. Папка .next-hints-review оставлена после запрета удаления — не удалять обходным способом. Отдельная goofy-maxwell-hints-review остаётся на e8148cc с junction зависимостей; она не текущая main. Состояние панели — project-control/state.json; после существенного изменения обновить overview и интерактивный снимок. История и ранее принятые ревью нужны для адресной проверки, а не для повторной загрузки всей переписки.

---

### Последнее исправление (19 сентября 2026, 22:52)

**Исправлено**: неправильное произношение Google TTS для фраз диалога урока 2 — «тода раба» звучало как «теуда раба», «бевакаша» — как «беваакаша».

**Коммит**: `96be972` — `fix(speech): strip nikkud before Google TTS URL`

**Первопричина**: коммит `a98b91d` (4 сентября) убрал `stripNikkud()` из `playFallbackAudio`, чтобы сохранить огласовки для союза «вэ». Побочный эффект: Google Translate TTS `translate.google.com/translate_tts` не умеет читать огласованный иврит. Проявилось активнее после `fc6574d` (18 сент.), который убрал `setTimeout(15ms)` — браузерный `speechSynthesis` стал чаще падать и переключаться на fallback.

**Решение**: `stripNikkud()` применяется строго для формирования `ttsText` перед URL Google TTS (только `lang==='iw'`). `cleanText` с огласовками сохраняется для `isQuestion`-детекции и браузерного `speechSynthesis`, который огласовки понимает.

**Тесты**: 218/218 pass. `runtime-normative-vav.test.cjs` обновлён — ожидания для fallback URL скорректированы.

---

### Обновление видеопроизводства (20 сентября 2026, 08:03)

**Задача:** Обучающий How-To скринкаст Этапа 5: Диалоги (`public/demo/tutorials/stage_05_dialogue_v2.mp4`).

**Что сделано и проверено:**
1. **Профиль и гендер (R-17):** В скрипте записи исправлен ключ `localStorage` (`hebrew_app_profile_v1` вместо `ulpana_user_profile`, убран `id`, вызывавший перезапись гостевым женским профилем в `page.tsx`). В видео отображается правильный мужской контекст: селектор `זָכָר`, реплика баристы *«שָׁלוֹם! בָּרוּךְ הַבָּא. מָה תִּרְצֶה לִשְׁתּוֹת?»*, роль `Вы: Посетитель`.
2. **Дикция и ударения русского диктора (R-25):** Локализована причина искажения вступительной фразы — движок Edge TTS (`ru-RU-DmitryNeural`) не воспринимает Unicode combining acute `\u0301` в русском тексте и ломал токенизацию слов («Эд», «диалаги»). Вступительная фраза заменена на чистый утверждённый вариант без спецсимволов: *«Пятый этап — диалоги. Здесь ты учишься говорить связно в реальных ситуациях без стресса.»*.
3. **Итоговое видео:** 36 секунд, формат 9:16 (390×844 @2x), 1.24 MB. Записано на живом DOM `http://localhost:3000/#lesson-2/chat` со 100% синхронизацией кликов и звука. Контрольные кадры 0.5s, 11.5s, 20s проверены.
4. **Контроль матрицы:** `npm run audit:intent` пройден на 100%, все инварианты зеленые.

---

### Обновление автопостинга YouTube Data API v3 (20 сентября 2026, 08:05)

**Задача:** Сквозной автопостинг видео и Shorts на YouTube (локальный CLI + веб-панель боевого сервера `/admin`).

**Что сделано и проверено:**
1. **OAuth 2.0 & Refresh Token:**
   - Настроен локальный мастер авторизации `node growth/scripts/post_to_youtube.cjs --auth` на порту 8085.
   - Пользователь успешно авторизовал приложение через Google OAuth consent screen.
   - Получен постоянный `YOUTUBE_REFRESH_TOKEN`, сохранён в `.env.local`.
   - Проверено подключение к Google API: канал `Ulpana` (ID: `UC1kWxNhUydNncIRzTbBzWJw`).
2. **Тестовая сквозная загрузка (Unlisted):**
   - Выполнен запуск `node growth/scripts/post_to_youtube.cjs --privacy=unlisted --send --register`.
   - Видео `public/demo/reels_duolingo_vs_reality.mp4` успешно загружено на YouTube через Resumable Upload.
   - Получен ID видео: `IqBfDfWt9T0`.
   - Ссылка на видео: `https://youtu.be/IqBfDfWt9T0`, Shorts: `https://www.youtube.com/shorts/IqBfDfWt9T0`.
   - Публикация автоматически зарегистрирована в `growth/data/publications.json` (`pub-yt-6375`).
3. **Боевой сервер (Vercel):**
   - Роут `src/app/api/admin/marketing/publish/route.ts` готов к 1-клик публикации прямо из браузера в `/admin`.
   - Роут `src/app/api/admin/marketing/health/route.ts` мониторит статус подключения YouTube канала.
   - Для работы на продакшене требуется внести 3 переменные в Vercel Project Settings -> Environment Variables: `YOUTUBE_CLIENT_ID`, `YOUTUBE_CLIENT_SECRET`, `YOUTUBE_REFRESH_TOKEN`.
4. **Тесты и матрица:**
   - 222/222 тестов успешно пройдены (`npm test`).
   - `npm run audit:intent` — 100% зелёный, 0 нарушений.

---

### Релиз Студии озвучки фраз и 2 475 предложений Google Cloud TTS (21 сентября 2026)

**Задача:** Студия озвучки предложений в `/admin`, предварительная генерация всех 2 475 фраз курса через Google TTS, переключатель движка озвучки предложений (R-14, R-17, R-24).

**Коммит:** `eca5d73` — `feat(admin): add sentences audio studio and pre-generate 2,475 Google TTS phrases`
**Выгрузка:** Отправлено в `origin main`, автосборка Vercel (`https://ulpana-hebrew.vercel.app`).

**Что сделано и проверено:**
1. **Студия озвучки в `/admin` (`AdminAudioSentencesTab.tsx`):**
   - Плоская таблица всех 2 475 предложений со столбцами: №, Фраза на иврите с огласовками, Русский перевод, Категория, «Текущий звук» (Web Speech API) и «Google Cloud TTS» (студийный MP3).
   - Пагинация (25 / 50 / 100 на страницу), живой поиск по ивриту и русскому, фильтры статуса («Все», «Только без MP3», «Только с MP3») и селектор категорий (глаголы, существительные, прилагательные, предлоги, мамы).
   - Чекбоксы выбора, генерация по 1 фразе и пакетная генерация с индикатором прогресса и отменой.
   - Мастер-переключатель движка: `Текущий синтез (Web Speech API)` vs `Google Cloud TTS (студийные MP3)` с мгновенным сохранением в `src/data/audioSettings.json`.
2. **Предварительная генерация аудио (`scripts/generate_all_sentences.cjs`):**
   - Сгенерированы 100% фраз курса (2 475 предложений, 2 428 уникальных MP3 суммарным объёмом 43.68 МБ).
   - Детерминированное хеширование `s_${hash}.mp3` исключило коллизии имен (0 коллизий).
   - Реестр сопоставления сохранён в `public/audio/sentences/manifest.json`.
   - Изоляция: слова мастер-словаря Pealim SSOT не затронуты (R-01, R-24).
3. **Верификация перед деплоем (R-14):**
   - `npm test`: 240 / 240 tests pass (100%).
   - `npm run typecheck`: 0 ошибок TypeScript.
   - `npm run build`: 45 / 45 страниц и API-роутов успешно собраны в Next.js 16.3.5.
   - `npm run audit:intent`: 100% Zero-Drift compliance, все 2 535 карточек ComplexDrills на месте.

---

### Релиз: Глобальное разделение промокодов (Общие vs Публикации) и спец-доступ для мам (21 сентября 2026)

**Задача:** Разделение промокодов на общие (закреп, био) и спецкоды под конкретные посты с привязкой ссылки на публикацию (`post_link`). Реализация льготного доступа для мам по промокодам `LATTE_MAMA`, `TG_MAMA`, `MOMS` (R-14, R-23).

**Коммит:** `eca5d73` / `3c3ec0f` — синхронизировано с `origin main`, автодеплой Vercel.

**Что сделано и проверено:**
1. **Льготный доступ для мам (`src/lib/permissions.ts`):**
   - Хелпер `isMomPromo` расширен на поддержку `LATTE_MAMA`, `MOMS`, `TG_MAMA`.
   - При активации кода мамы получают вечный бесплатный доступ ко всем 7 колодам родительской тематики («Детский сад», «Школа», «Врач/Поликлиника», «Игровая площадка», «Семья», «Родители и дети») + 30 дней полного PRO на всё приложение (диалоги, звонки, 100 уроков).
2. **Архитектура базы данных и API (`src/lib/db.ts`, `src/app/api/admin/promos/route.ts`):**
   - Таблица `ulpana_promo_codes` дополнена полями: `code_type` (`general` | `post`), `channel` (`tg` | `fb` | `insta` | `tiktok` | `yt` | `other`), `post_link` (URL), `description`.
   - API поддерживает чтение, создание и валидацию новых метаданных.
   - Автосидинг промокодов обновлён для `LATTE_MAMA` (FB, post), `TG_MAMA` (TG, post), `MOMS` (other, general).
3. **Интерфейс панели администратора (`/admin`):**
   - В карточке создания и в модальном окне добавлены: переключатель типа («🌐 Общий канал» / «🎯 Под публикацию»), выбор платформы, поле ввода URL публикации (при типе `post`), поле описания.
   - В таблице активных промокодов внедрены: фильтры по типу и платформе, счётчик отфильтрованных кодов, бейджи канала и типа, кликабельная ссылка «Открыть публикацию» (`ExternalLink`), текст описания кампании.
4. **Контент для промо-поста в «Тыквенный латте»:**
   - Подготовлен искренний нерекламный текст Сергея о личном опыте семьи, тяготах адаптации мам в Израиле и бесплатном доступе по коду `LATTE_MAMA`.
   - Сгенерирован аутентичный визуальный вариант №3 (мама и ребёнок у ворот израильского детского сада с надписью «גן ילדים», утреннее солнце, 35mm плёночный стиль).
5. **Верификация (R-14):**
   - `node --test tests/mom-decks-permissions.test.cjs tests/decision-matrix-invariants.test.cjs`: 17 / 17 passed.
   - `npm run typecheck`: 0 ошибок.
   - `npm test`: 240 / 240 passed.
   - `npm run build`: 45 / 45 pages успешно скомпилированы.
   - `npm run audit:intent`: 100% соблюдение матрицы.

---

### Релиз: Пакеты бессрочного доступа (Access Bundles) и ликвидация хардкода промокодов (21 сентября 2026)

**Задача:** Реализация архитектурного варианта №2: возможность гибко выбирать уроки и типы колод, которые промокод открывает навсегда (независимо от срока PRO); ликвидация любого хардкода промокодов (`LATTE_MAMA`, `TG_MAMA`, `MOMS`) в коде разрешений и карточках; создание отдельной таблицы бандлов в БД и визуализация в админ-панели (R-14, R-23).

**Что сделано и проверено:**
1. **Модуль `src/lib/promoBundles.ts`:**
   - Единый источник истины для пакетов доступа: `DEFAULT_BUNDLES` (`bundle_moms`, `bundle_lessons_1_30`, `bundle_professional`, `bundle_all_free`).
   - Функции резолвинга бандлов и сопоставления контента: `resolvePromoBundle`, `isPromoUnlockingMomDecks`, `isDeckUnlockedByBundle`, `isLessonUnlockedByBundle`.
2. **База данных (`src/lib/db.ts`):**
   - Новая таблица `ulpana_promo_bundles` (`id`, `name`, `description`, `icon`, `unlocked_lessons`, `unlocked_decks`, `unlocked_categories`).
   - Расширение таблицы `ulpana_promo_codes` полями `bundle_id`, `unlocked_lessons`, `unlocked_decks`, `unlocked_categories`.
   - Расширение таблицы `ulpana_users` полями `activated_promos`, `unlocked_decks`, `unlocked_categories`, `unlocked_lessons` для перманентного сохранения прав даже после окончания PRO.
3. **Ликвидация хардкода (`src/lib/permissions.ts`, `DeckCard.tsx`, `ThematicDecksView.tsx`):**
   - Удалены все хардкодные строковые проверки `LATTE_MAMA`, `TG_MAMA`, `MOMS`.
   - Проверка прав выполняется динамически через профиль пользователя (`userProfile.unlockedCategories`, `userProfile.unlockedDecks`, `userProfile.unlockedLessons`) и `resolvePromoBundle()`.
4. **Админ-интерфейс (`/admin`):**
   - На вкладке «Управление доступом» добавлена интерактивная таблица «Пакеты бессрочного доступа (Access Bundles)» с привязанными кодами и быстрым переходом к генерации.
   - В конструкторе промокодов добавлены выбор системного пакета и режим ручной настройки категорий и диапазонов уроков («⚙️ Настроить темы»).
   - В таблице промокодов выводится бессрочно открываемый контент.
5. **Верификация (R-14):**
   - `node --test tests/mom-decks-permissions.test.cjs tests/promo-bundles.test.cjs`: 10 / 10 passed.
   - `node --test tests/decision-matrix-invariants.test.cjs`: 13 / 13 passed.
   - `npx tsc --noEmit`: 0 ошибок.
   - `npm run audit:intent`: 100% Zero-Drift compliance.

---

### Релиз: Конвейер производства 100 видеороликов уроков (Lesson Showcase Engine) и эталонное видео Урока 7 (21 сентября 2026)

**Задача:** Создание конвейера и инфраструктуры для 100 обучающих вертикальных видеороликов курса «Ульпан Алеф» (9:16, до 60 секунд) со сквозной демонстрацией связки «Словарь (этап 2) $\rightarrow$ Диалог (этап 5) $\rightarrow$ Звонок с ИИ (этап 6 как лид)». Создание эталонного видеоролика Урока 7 («Аренда квартиры в Израиле») с реальным экраном приложения, кастингом голосов, студийным аудио, уникальными промокодами и шаблонами описаний под каждую платформу (R-23, R-25).

**Что сделано и проверено:**
1. **Регламент производства 100 уроков (`growth/LESSON_SHOWCASE_PLAYBOOK.md`):**
   - Зафиксирована 4-актная структура ролика (<60с): Акт 1 (0:00–0:11, Словарь), Акт 2 (0:12–0:26, Диалог с живым микрофоном и оценкой 98%), Акт 3 (0:26–0:39, Звонок с ИИ как лид-магнит), Акт 4 (0:39–0:47, CTA с промокодом на 7 дней).
   - Платформенная матрица промокодов: `YOUTUBE` (Shorts), `INSTA` (Reels), `TIKTOK` (TikTok), `TG` (Telegram), `FB` (Facebook Reels).
2. **Студийное мастер-аудио (`growth/scripts/generate_lesson_07_audio.mjs`):**
   - 11 сведенных речевых дорожек (диктор `ru`, басовитый Эли `iw`, контрастный ученик `iw`), телефонный гудок звонка и перезвон успеха (chime).
   - Косинусное сглаживание 15ms (de-clicking), частота 44.1 kHz, стерео 16-bit PCM.
   - Файл: `public/demo/audio_cache/l07_master_audio.wav` (ровно 47 секунд, 2 072 700 сэмплов).
3. **Запись реального экрана в Playwright (`growth/scripts/record_lesson_07_video.mjs`):**
   - Честный пошаговый UI `http://localhost:3000/#lesson-7/vocab` в мобильном вьюпорте 390x844 (9:16).
   - Интерактивный курсор тач-кликов с анимацией пульсации.
   - Чистый экран без Next.js dev overlay (`[data-nextjs-dev-overlay]`, `nextjs-portal`).
   - Точная покадровая синхронизация тапов с речевыми кью: переключение в «Списком», подсветка `דִּירָה`, `חֲדָרִים`, `מְקָרֵר`, переход в Диалог, выбор роли Б, живая запись ответа, бейдж 98% «Отлично!», переход в звонок, исходящий вызов и активный телефонный HUD с Эли.
4. **Платформенные варианты (`growth/scripts/render_lesson_07_variants.mjs`):**
   - Сгенерированы все 6 видеофайлов в `public/demo/lessons/`:
     - `lesson_07_master.mp4` (1.99 MB, 47 сек)
     - `lesson_07_youtube_shorts.mp4` (1.99 MB, промокод `YOUTUBE`)
     - `lesson_07_instagram_reels.mp4` (1.85 MB, промокод `INSTA`)
     - `lesson_07_tiktok.mp4` (1.85 MB, промокод `TIKTOK`)
     - `lesson_07_telegram.mp4` (1.85 MB, промокод `TG`)
     - `lesson_07_facebook_reels.mp4` (1.85 MB, промокод `FB`)
5. **Шаблоны описаний и диплинки (`growth/content/lesson_07_descriptions.md`):**
   - Готовые тексты со словарем урока (`דִּירָה`, `חֲדָרִים`, `מְקָרֵר`, `מִיטָּה`, `סָלוֹן`, `יֵשׁ/אֵין`), UTM-метками и промокодами для каждой из 5 платформ.
6. **Покадровая визуальная верификация и тесты (R-13, R-25):**
   - Извлечены и проверены 4 контрольных кадра через `view_file`: словарь (`frame_01_vocab.png`), диалог 98% (`frame_02_dialogue.png`), звонок с Эли (`frame_03_phone.png`), промо-оверлей (`frame_04_promo.png`).
   - `npm test`: 251 / 251 passing (100%).
   - `npm run audit:intent`: 100% Zero-Drift compliance, все критические инварианты матрицы соблюдены.

---

### Реестр озвучки предложений курса: Двухголосый синтез и полный реестр 2 688 файлов (21 сентября 2026)

**Задача:** Подготовка полного набора предложений курса (2 688 аудиотреков) для сторонней студийной/нейросетевой озвучки с разделением по голосам: мужская речь — мужским диктором, женская речь — женским диктором, все нейтральные/безличные фразы — женским диктором (R-17).

**Что сделано и проверено:**
1. **Анализ грамматических категорий и исправление adapter (`src/lib/drills/sentenceGenderAdapter.ts`):**
   - Приоритет проверки 3-го лица мужского рода (`הוא`, `דוד`) и женского рода (`היא`, `שרה`) поднят выше проверки 2-го лица.
   - Устранено ложное срабатывание на предлог винительного падежа `אֶת` (et), ранее ошибочно относившее мужские фразы вроде «הוּא קָרָא אֶת הַחֲדָשׁוֹת» к женскому роду.
   - Дефолтный голос для нейтральных предложений установлен на женский (`he-IL-HilaNeural`) по требованию архитектуры.
2. **Точная раскладка 2 688 аудиотреков:**
   - **565 мужских аудиотреков (MALE):** 1-е лицо м.р. (211), 2-е лицо м.р. (29), 3-е лицо м.р. (325).
   - **2 123 женских аудиотреков (FEMALE):** женские формы 1-го/2-го лица (213), 3-е лицо ж.р. (78), 2-е лицо ж.р. (2), нейтральные/безличные/существительные/предлоги (1 830).
3. **Сгенерированные экспортные файлы в `public/`:**
   - `public/sentences_full_registry.txt` (2 688 строк: индекс, пол, имя файла, огласованный иврит, транскрипция, русский перевод).
   - `public/sentences_male.txt` (565 строк: нумерованный огласованный иврит для загрузки в мужской TTS).
   - `public/sentences_female.txt` (2 123 строки: нумерованный огласованный иврит для загрузки в женский TTS).
   - `public/sentences_male_manifest.tsv` и `public/sentences_female_manifest.tsv` (для точной привязки сгенерированных mp3).
   - `public/sentences_full_registry.csv` и `.tsv` (полная база для Excel/Sheets с UTF-8 BOM).
4. **Верификация (R-13, R-17):**
   - `npm test`: 251 / 251 passing (100%).
   - `npm run audit:intent`: 100% соблюдение матрицы решений.

---

### Релиз: Ликвидация робота в закадре, ИИ-имитация звонка без сбоев связи и фиксация 30-дневного триала (21 сентября 2026)

**Задача:** Ликвидация причин механического роботизированного звучания русского закадра в видеороликах; устранение ошибки связи («сбой связи») во время записи звонка с Эли; педагогическое перепозиционирование Этапа 6 со сбивчивого «реальный звонок» на точное «имитация звонка с ИИ без страха»; полная синхронизация коммерческого оффера на **30 дней бесплатного триала** (R-15, R-17, R-23, R-25).

**Что сделано и проверено:**
1. **Диагностика и ликвидация древнего TTS-робота:**
   - Установлена первопричина механического звука: `tryGeminiTts` в цикле опрашивал бесплатный ключ Gemini, натыкался на 429 Too Many Requests (10 RPM), молча глотал ошибку и падал в `scripts/tts_helper.mjs` на устаревший Google Translate TTS `tw-ob` образца 2010 года.
   - Генератор аудио полностью переведён на нейросетевой комплекс Microsoft Azure Neural TTS (`msedge-tts`):
     - Русский закадр: **`ru-RU-DmitryNeural`** (глубокий, теплый, уверенный голос студийного диктора).
     - Иврит: **`he-IL-AvriNeural`** (хозяин Эли, слова) и **`he-IL-HilaNeural`** (ученица).
2. **Педагогическое позиционирование (Имитация звонка с ИИ):**
   - Формулировка «реальный звонок» заменена на точную: *«А в симуляторе звонка тренируем разговор с ИИ-хозяином без стресса:»*.
3. **Ликвидация сбоя связи в UI звонка:**
   - В `growth/scripts/record_lesson_07_video.mjs` добавлены E2E-моки для `/api/ai/phone` и `/api/ai/phone/debrief`.
   - В `src/components/PhoneCallSimulator/PhoneCallSimulator.tsx` добавлен тестовый хук `window.__phoneSimulatorSendMessage`.
   - Реплика ученика (`שָׁלוֹם! יֵשׁ כְּבָר מְקָרֵר וּמִיטָּה?`) и мгновенный ответ Эли (`בֶּטַח! בּוֹא לִרְאוֹת הַיּוֹם בְּשֵׁשׁ.`) синхронизированы с аудиодорожкой. Плашка «сбой связи» полностью исчезла, звонок завершается штатным нажатием красной трубки.
4. **Фиксация 30-дневного триала:**
   - Ошибочные упоминания «3 дня» и «7 дней» удалены.
   - В аудио: *«Забирай словарь в описании и тренируй иврит 30 дней бесплатно!»*.
   - В оверлее CTA: *«ПРОМОКОД НА 30 ДНЕЙ ПРЕМИУМА»*.
   - В описаниях уроков для всех платформ: *«30 дней бесплатного премиума»*.
5. **Характеристики собранного мастер-видео:**
   - Файл: `public/demo/lessons/lesson_07_master_youtube.mp4`.
   - Хронометраж: ровно **57.80 секунды** (строго < 60с).
   - Разрешение: 390x844 @2x (9:16 vertical), 30 fps, H.264 High.
6. **Верификация (R-13, R-25):**
   - `npm test`: 251 / 251 passed (100%).
   - `node --test tests/decision-matrix-invariants.test.cjs`: 13 / 13 passed.
   - `npm run audit:intent`: 100% Zero-Drift compliance.

---

### Релиз: Полная миграция аудиобанка курса на Microsoft Azure / Edge Neural TTS (22 сентября 2026)

**Задача:** Ликвидация дефектов синтеза Google Cloud Chirp 3 HD (проглатывание согласных `ם`, искажение сеголатных ударений `מַיִם`, искажение имён `דָּוִד`, сжатие фраз до 0.4с); перевод 100% предложений курса (2 688 файлов) на эталонный нейросетевой комплекс Microsoft Azure / Edge Neural TTS (`he-IL-AvriNeural` ♂, `he-IL-HilaNeural` ♀); фиксация движка в качестве основного стандарта платформы (R-13, R-17, R-24).

**Что сделано и проверено:**
1. **Диагностика дефектов Chirp 3 HD:**
   - Выявлена первопричина: сырой токенизатор Chirp 3 HD сбоил на Unicode-символах огласовок `\u0591-\u05C7`, ускорял речь в 3-4 раза и выкидывал согласные буквы. Средний размер файлов составлял всего 7-8 КБ.
2. **Переход на Microsoft Azure / Edge Neural TTS:**
   - Скрипт `scripts/generate_from_manifests.cjs` переведён на нейросетевой комплекс Microsoft с прямой поддержкой огласованного иврита.
   - Сгенерировано **2 688 MP3-файлов** (общий объём базы вырос с 19.2 МБ до **41.80 МБ**, средний размер файла — 15–16 КБ, естественная длительность 2.3–2.8с):
     - ♂ **565 мужских фраз:** голос `he-IL-AvriNeural` (1-е/2-е/3-е лицо мужского рода, включая контрольные фразы «אֲנִי רוֹצֶה מַיִם קָרִים», «דָּוִד הוֹלֵךְ לָאוּלְפָּן»).
     - ♀ **2 123 женских и нейтральных фраз:** голос `he-IL-HilaNeural` (женские варианты 1-го/2-го лица, 3-е лицо ж.р., все существительные, прилагательные, предлоги и безличные фразы).
3. **Обновление системных настроек и манифеста:**
   - `src/data/audioSettings.json`: `"sentenceAudioEngine": "edge_neural"`, `"defaultVoiceMale": "he-IL-AvriNeural"`, `"defaultVoiceFemale": "he-IL-HilaNeural"`.
   - `src/lib/speech.ts`: дефолтный возврат `getSentenceAudioEngine()` установлен в `'edge_neural'`.
   - `public/audio/sentences/manifest.json`: синхронизирован со всеми 2 688 файлами.
   - `DECISION_MATRIX.md`: обновлена до версии v1.7.0, отказ от Chirp 3 HD зафиксирован в Superseded Log.
4. **Верификация (R-13, R-17, R-22):**
   - `npm run audit:intent`: 100% Zero-Drift compliance (все 2 535 карточек ComplexDrills покрыты).
   - `npm run typecheck`: 0 ошибок компиляции TypeScript.
   - `tests/decision-matrix-invariants.test.cjs`: 13 / 13 passed.
   - `tests/audio-sentences-studio.test.cjs`: 4 / 4 passed.
   - `tests/pilot-mechanics-01-05.test.cjs`: 11 / 11 passed.

---

### Фикс: Ликвидация сбоя и зацикливания аудио при переходе между карточками в режиме «Комплекс» (23 сентября 2026)

**Проблема:** В режиме «Комплекс» (`ComplexDrillMode`) при переходе к следующей карточке (вручную или через автопереход) происходил сбой: звук воспроизводился некорректно, карточки неконтролируемо пролистывались, фазы сменялись за 0 миллисекунд и звук зацикливался.
**Первопричина:**
1. Вызов `stopSpeech()` ставил аудио на паузу и сбрасывал `.src`. Браузер реджектил промис `audio.play()` с `AbortError`. В `src/lib/speech.ts` обработчик `.catch` не проверял `AbortError` / флаг отмены и запускал нежелательный TTS-фолбэк `playWithTts()`.
2. При вызове `speechSynthesis.cancel()` событие `onerror('canceled')` мгновенно (за 0мс) резолвило промис озвучки фразы, вызывая стремительный прогон фазы паузы и переход к подтверждению.
3. В `ComplexDrillMode.tsx` экземпляр `new Audio` не сохранялся в ref, старый звук накладывался на новый, а таймеры интервалов не очищались при ручной навигации.
**Что сделано и проверено:**
1. В `src/lib/speech.ts` добавлен флаг `_cancelled` при остановке аудио в `stopSpeech()` и `speakRussian()`. В `speakHebrew()` добавлен фильтр отмены и `AbortError` в `playPromise.catch` и `onerror`, блокирующий ложный запуск TTS при намеренном прерывании карточки.
2. В `src/components/FlashcardTrainer/modes/ComplexDrillMode.tsx`:
   - Внедрен `currentAudioRef` для контроля за `HTMLAudioElement` фразы.
   - Добавлен метод `stopAllDrillAudio()` с установкой `_cancelled = true`, `.pause()`, сбросом `.src` и вызовом `stopSpeech()`.
   - В `playHebrewSentence()` добавлена проверка актуальности `playCycleIdRef` после получения MP3 и фильтрация `AbortError`.
   - Внедрены навигационные обёртки `handleAdvance` и `handlePrev` с инкрементом `playCycleIdRef.current += 1`, очисткой интервалов и отменой аудио. Кнопки интерфейса переведены на эти обработчики.
   - Взаимодействия с токенами и словами корня переведены на `stopAllDrillAudio()`.
3. В `src/components/FlashcardTrainer/FlashcardTrainer.tsx` в обработчики навигации (`handleAdvanceNext`, `handlePrevWord`, `handleNextWord`) добавлен вызов `stopSpeech()`.
4. Создан регрессионный тест `tests/complex-drill-lifecycle.test.cjs` (3/3 passed).
5. Верификация: `npm test` (289/289 passed), `npm run audit:intent` (100% зелёный статус, 2535/2535 слов покрыты в ComplexDrills).

---

### Фикс: Устранение зацикливания/отката карточек между индексами во всех режимах колод (23 сентября 2026)

**Проблема:** При переходе к следующей карточке в любом режиме в колоде («Комплекс», «Слово», «Тест», «Спринт» и др.) происходило циклическое переключение (bounce) между предыдущей и следующей карточкой, сопровождаемое перезапуском звука и невозможностью перейти дальше.
**Первопричина:**
В `src/components/FlashcardTrainer/FlashcardTrainer.tsx` хук синхронизации внешнего URL-параметра `initialCardIndex` имел `currentIndex` в массиве зависимостей:
```ts
useEffect(() => {
  if (typeof initialCardIndex === 'number' && initialCardIndex >= 0 && initialCardIndex !== currentIndex) {
    setCurrentIndex(initialCardIndex);
  }
}, [initialCardIndex, currentIndex, words.length]);
```
При нажатии «Далее» внутренний `currentIndex` менялся (например, с 0 на 1). В том же кадре хук немедленно перезапускался из-за изменения `currentIndex`, но проп `initialCardIndex` от родителя (`page.tsx`) ещё оставался равен `0`. Условие `0 !== 1` срабатывало, и хук вызывал `setCurrentIndex(0)`, моментально возвращая карточку назад на 0. В следующем тике родитель успевал отреагировать на `onCardChange` и передать `initialCardIndex = 1`, что вызывало повторный бросок карточки на 1. Возникал бесконечный асинхронный пинг-понг между карточками с перезапуском аудио.

**Что сделано и проверено:**
1. В `src/components/FlashcardTrainer/FlashcardTrainer.tsx`:
   - Внедрены рефы `lastReportedIndexRef` и `prevInitialCardIndexRef`.
   - В `onCardChange` фиксируется актуально отправленный наверх индекс: `lastReportedIndexRef.current = safeIndex`.
   - Из массива зависимостей хука синхронизации исключён `currentIndex`: зависимости стали `[initialCardIndex, words.length]`.
   - Добавлен барьер от эхо-отката (`echo guard`): переход по `initialCardIndex` применяется ТОЛЬКО если проп действительно изменился извне (`initialCardIndex !== prevInitialCardIndexRef.current`) И не является эхом нашего собственного перелистывания (`initialCardIndex !== lastReportedIndexRef.current`).
2. В `tests/flashcard-deep-routing.test.cjs` добавлен тест 4:
   - Проверяет наличие `lastReportedIndexRef`, `prevInitialCardIndexRef`, echo guard и отсутствие `currentIndex` в зависимостях эффекта.
3. Верификация:
   - `tests/flashcard-deep-routing.test.cjs`: 4/4 passed.
   - `npm test`: 290/290 passed (100%).
   - `npm run audit:intent`: 100% Zero-Drift, все критические инварианты зелёные.


