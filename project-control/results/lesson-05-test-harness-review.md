# Доработка тестового окружения урока 5

14 сентября около 04:20. Текущая задача ещё не принята; main не изменён.

Независимый запуск нового теста с пределом 15 секунд прошёл первые две проверки, затем завершился с JavaScript heap out of memory примерно через 29 секунд. Другой запускавшийся PowerShell сообщил о нехватке памяти. Диагностический процесс архитектора уже завершился с кодом 1; повторять этот запуск без изменения причины нельзя. Не оставляй свои зависшие диагностические процессы; останавливай только собственные задачи, чужие процессы не трогай.

Подтверждённое противоречие теста реализации: `LessonExercises.tsx` около строки 911 показывает нижнюю кнопку перехода при `isAnswered`, включая неправильный ответ. В новом тесте `assert.equal(nextEx2BtnWrong, undefined)` требует отсутствия этой реально существующей кнопки. Это неверное ожидание; изменение поведения компонента ради теста не нужно. Различать ошибку и успех по фактическому заголовку, наличию/отсутствию кнопки повторения и смене состояния, как в предыдущем ревью. Кнопка «Следующий вопрос» может существовать при обоих исходах.

Рабочая гипотеза причины расхода памяти: неуспешный assert сравнивает огромный DOM-объект с undefined, и форматирование ошибки обходит React/DOM-ссылки. Подтвердить короткой диагностикой без печати/inspect целого DOM; сравнивать булево наличие, строки и числа. Не маскировать сбой catch или подменой оценивания.

Блок с локальными константами baselineEx5_2/6/7 и assert.notEqual лишь проверяет неизменяемые константы. Убрать этот блок из регрессионного теста; настоящий отрицательный прогон выполнит архитектор подстановкой модуля e8148cc в процесс. Не тратить время на повторную ручную копию старого задания.

После устранения ошибочного ожидания и зависания завершить первоначальные проверки и отчёт. Не открывать новую задачу, не менять рабочий компонент только ради окружения jsdom.
