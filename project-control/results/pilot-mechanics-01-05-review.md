# Независимое ревью исправлений механик пилота

14 сентября 2026, около 00:23, Asia/Jerusalem. Архитектор: ChatGPT. Замечания отправлены Gemini через Antigravity в 00:23; появление сообщения, пустой редактор и Working подтверждены.

Проверяемая версия: `3bf5a13387c06af9ade978970cc94e16f2c052dc`, ветка `fix/pilot-mechanics-01-05`, рабочая копия `C:\Users\azrie\Documents\antigravity\goofy-maxwell-mechanics`. Основной checkout не изменён реализацией. Статус: требуется небольшая доработка и завершение проверок; приёмки пока нет.

## Подтверждённые замечания

1. В женском сценарии урока 5 `completionCondition` заканчивается мужским «или спросил машину». Согласовать всё предложение («…или спросила о машине»); проверить полное условие, а не только два первых фрагмента. Мужской сценарий не менять в этой задаче.
2. В отчёте отсутствуют результаты обязательных `build` и аудита зависимостей из DEVELOPMENT.md. После доработки выполнить требуемые проверки, включая lint критических компонентов, сообщить точные результаты. Наличие `hebrewSnippet` в данных не доказывает фактический вызов озвучки; SSR-тест подтверждает начальный экран, но не интерактивный ответ/повтор. Проверить эти переходы локально и честно отделить автоматические проверки от ручных и непроверенных.
3. Поправить отчёт: ссылка на пункт 47 исходного ревью неверна, смешанные алфавиты в цитатах «מֵרוּסְיָя» и «אни» отсутствуют в проверяемом коде. Цитаты должны совпадать с данными. Удалить неиспользуемые импорты в новом тесте (`adaptGenderInScenario`, `BESPOKE_PHONE_SCENARIOS`) и ненужный экспорт помощника, если публичный API достаточно покрывает поведение.

## Уточнение языкового вывода архитектора

Первоначальное предположение архитектора, что `гарá` само по себе ошибочно, отозвано после обращения к источнику. [Pealim: לָגוּר](https://www.pealim.com/ru/dict/4-lagur/) отдельно указывает разговорный перенос ударения с окончания. Нельзя автоматически заменять нормативное настоящее `гарá` на разговорное `гáра` только потому, что последнее встречается в старых данных.

Подтверждена существующая несогласованность курса: `src/data/lessons/alef_01_10.ts` (грамматическая таблица около строки 907, словарь 956, объяснение 1176) и `essayTopics.ts` используют `гáра`, а `dialogues_01_10.ts` и `dialogueUtils.ts` — `гарá`. В этой задаче сохранить `гарá`; согласование регистра, таблицы и остальных механик вынести в отдельную задачу по языковому стандарту. Пользователь требует современный академически корректный иврит; нормативные и разговорные варианты следует обозначать, а не смешивать молча.

## Что уже проверено чтением кода

- Мужские сценарии не проходят блок женских переопределений; изменения ограничены номерами 2–5.
- Имя и роль Сары сохранены. Обращение ученицы к мужчине-водителю `לְךָ` не заменено на женское.
- Словарная фраза ожидания содержит маркировку говорящей женщины и адресата-мужчины; это не изменение пола водителя в его репликах.
- Заголовок аудирования теперь получает нейтральную инструкцию до ответа. Обработчик кнопки сохраняет `hebrewSnippet` и прежний резервный источник; варианты ответа не изменены.
- Общий компонент влияет на аудирование всех уроков. Это шире пилота и отражено в отчёте. Остальные телефонные сценарии не переписывались.
- Повторяющиеся плашки и темы уроков в diff отсутствуют.

## Второй проход: 14 сентября, 00:33

Проверен фактический HEAD `1901bfe85a4837057fe441fda54b82c359b34ded`. Женское условие урока 5 исправлено полностью; ненужные импорты и экспорт удалены. Эти замечания закрыты.

Независимо запущены архитектором на этой версии:

- `npm test`: 66/66, код 0. Само прохождение не подтверждает заявленный интерактивный охват — см. ниже.
- `npm run typecheck`: код 0.
- `npm audit --audit-level=high`: 0 уязвимостей, код 0.
- `npm run build` (`next build --webpack`): код 0, 33/33 страницы. Получены предупреждения о различии `C:`/`c:` в путях модулей. `node_modules` рабочей копии — junction к основному checkout с путём `c:`. Это зафиксированное ограничение окружения, а не основание переписывать код приложения.
- Lint пяти файлов (AuthModal, FlashcardTrainer, useAiChat, LessonExercises, phoneScenarios): 0 ошибок, 2 существующих предупреждения неиспользуемых `titleLower`/`catLower` в phoneScenarios:752–753. Утверждение отчёта «0 warnings» не подтверждено.

### Осталось до приёмки

1. Три добавленных теста после строки 277 не являются заявленной проверкой взаимодействия с компонентом. Первый вызывает чистую функцию с `false`, `true`, `false`, не выбирая ответ и не нажимая повтор. Второй копирует выражение выбора источника звука. Третий копирует обработчик из приложения прямо в тест и сам вызывает подставленную `speakHebrew`. Если удалить настоящий onClick или сброс `isAnswered` из компонента, эти тесты продолжат проходить. Это запрещённое правилами проекта зеркалирование реализации, а не доказательство поведения.
2. Заменить эти три теста проверкой реально смонтированного компонента/локального интерфейса. Уже установлен Playwright; не нужен доступ к рабочей БД или платному ИИ. Нажать настоящую кнопку аудио, перехватив только границу TTS; выбрать ошибочный ответ, проверить раскрытие вопроса и ошибку; нажать повтор и проверить сокрытие; выбрать правильный ответ. Проверить также пропуск/возврат. Показать, что проверка обнаруживает поломку настоящего обработчика/сброса состояния (временные мутации откатить до commit). Объём — минимум реальный сценарий пилота и один за его пределами; не создавать публичный тестовый маршрут в продукте.
3. Исправить отчёт по фактам: полный hash в нём не совпадает с Git (`1901bfe053…` вместо `1901bfe85…`), команды `test` и `build` переписаны неточно, «все 100 уроков гарантированы» не следует из выборки. Копировать команды и результаты из фактических запусков, указывать границы выборки. Физическое качество произношения TTS остаётся отдельной непроведённой проверкой.

Статус: функциональная правка выглядит соответствующей задаче; до приёмки требуется заменить недостоверные проверки и исправить отчёт. Боевой выпуск и merge не выполнялись.

### Уточнение способа проверки, около 01:04

Запуск полного Next-приложения через Chrome затянулся из-за инфраструктуры. Для локальной правки компонента достаточно настоящего React DOM-теста: смонтировать импортированный `LessonExercises` через `createRoot`, выполнять клики DOM-кнопок внутри `await act(...)`, проверять показанный текст, состояние ошибки/повтора и вызов внешней функции озвучки. Это соответствует первоначальному требованию «реально смонтированный компонент/локальный интерфейс»; копирование обработчиков или самостоятельное присваивание `isAnswered` не соответствует.

Разрешено добавить `jsdom` как devDependency из официального npm, совместимую с Node 24, с обновлением lockfile. [Документация jsdom](https://github.com/jsdom/jsdom) описывает DOM в Node; [React act](https://react.dev/reference/react/act) — завершение обновлений перед assertions. Не включать запуск Chrome/Next dev в общий быстрый `npm test`. Взять реальные упражнения уроков 2 и 6, при необходимости оставить два упражнения в props для проверки пропуска/возврата. Подставить только внешнюю границу `speakHebrew` и отсутствующий в DOM-эмуляторе scroll; не сам компонент, его hooks или логику выбора/повтора. В конце восстановить globals/mocks и размонтировать root, закрыть DOM.

Перед установкой зависимости убрать только junction node_modules своей рабочей копии и создать её собственные зависимости. Не устанавливать пакеты через junction в основной checkout. Сначала проверить абсолютный путь, удалять саму ссылку без рекурсивного удаления её целевого каталога. Сохранить существующие изменения кода.

Полный браузерный проход всего приложения и физическая проверка звука остаются отдельными проверками выпуска. Для этой задачи нужны надёжные компонентные взаимодействия и доказанное обнаружение поломки реального обработчика/сброса. Не продолжать бесконечные запуски dev-серверов ради этого контракта.

## Итоговая приёмка: 14 сентября, около 01:40

**Принято в границах задачи.** Проверенный commit `0674b160c23f38004347833f2edf5172ba3c7058`, ветка `fix/pilot-mechanics-01-05`. Новый отчёт содержит правильный полный hash и явно ограничивает интерактивную выборку уроками 2 и 6. Прежние разделы выше — история доработок, а не текущий отказ в приёмке.

Независимые проверки архитектора:

- `npm ci`: код 0, 446 пакетов установлены из lockfile, 0 уязвимостей. Рабочая копия имеет собственный node_modules. Получены предупреждения о поддержке прежнего ESLint и неразрешённом install-script unrs-resolver; проверки ниже реально выполнены без дополнительного разрешения этих скриптов.
- `npm test`: 65/65, код 0, без пропусков. Прочитана реализация: настоящий LessonExercises монтируется через createRoot; DOM-клики проходят через act; подставляется внешняя озвучка. Скопированных обработчиков в новых интерактивных тестах нет.
- Две независимые мутации применены только в памяти отдельного Node-процесса к фактическому исходнику компонента. Отключение первого вызова speakHebrew привело к двум ошибкам «No audio was spoken via speakHebrew». Удаление resetCurrentAnswerState из handleRetryCurrent привело к двум ошибкам сокрытия слова после повтора. Каждый отрицательный прогон завершился кодом 1. Исходники на диске не менялись.
- `npm run typecheck`: код 0. `npm audit --audit-level=high`: код 0, 0 уязвимостей.
- Целевой lint трёх обязательных компонентов и двух затронутых файлов: код 0, 0 ошибок и 2 прежних предупреждения titleLower/catLower.
- Первый `npm run build` с прежним `.next` завершился кодом 1: отсутствующая иконка lucide `book-bookmark.mjs`. После npm ci установленный lucide-react 1.33.0 такой файл и ссылку на него не содержит. Повтор с существующей настройкой `ULPANA_BUILD_DIR=work/architect-build-0674` и новым каталогом: код 0, компиляция 29.3 секунды, 33/33 страницы. Это подтверждает сборку чистого кандидата и указывает на несогласованность прежнего кэша; точный механизм его появления не установлен. Ошибка первого прогона сохранена в доказательствах.

Next добавил в tsconfig только пути к временному каталогу проверки. Diff проверен, эти два автоматически созданных include удалены восстановлением исходного tsconfig; рабочая копия снова чистая. Изменения принятого commit после проверок отсутствуют.

Около 01:40 основная локальная ветка main продвинута fast-forward с ec4d663 до 0674b16. Существующие изменения AGENTS.md/DEVELOPMENT.md и неотслеживаемые .github/project-control сохранены. Push, размещение и изменение боевого сайта не выполнялись.

Приёмка относится к женским телефонным подсказкам 2–5, сокрытию вопроса аудирования и указанным регрессиям. Она не закрывает Telegram-вход, языковой аудит всех уроков, сквозные браузерные сценарии или физическое качество TTS. Следующая ограниченная задача — сохранение нормативного /u/ в транскрипции; новые общие условия выпуска — project-control/beta-acceptance.md.
