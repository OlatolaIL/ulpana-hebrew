# Gemini: нормативный союз в живых диалогах и подготовке озвучки

Статус: фактически отправлено Gemini через Antigravity 14 сентября в 04:47 по Израилю. Сообщение, пустой редактор и чтение задания подтверждены. Не дублировать. База d413f9489f144599c4c95bd8321c9613b01a78db; ветка fix/runtime-normative-vav; новая отдельная рабочая копия C:\Users\azrie\Documents\antigravity\goofy-maxwell-runtime-vav. Код и тесты только там. Общие правила читать из основного goofy-maxwell. Main не менять. Передача проверена; ждать результат в той же задаче.

## Подтверждённая проблема

Это продолжение пункта 15, а не повтор принятого исправления нормализатора 6b397f6. Независимое воспроизведение на e8148cc: evidence/runtime-vav-before-e814.json, генератор runtime-vav-review.cjs.

- Реальный POST /api/ai/phone для обоих провайдеров превращает синтетический правильный ответ `וּגְבִינָה` / `у-гвинá` в `вэ-гвинá`, в основной реплике и suggestedReplies. Локальный sanitizeTranscription содержит принудительную замену.
- Фактический исходящий промпт телефона требует ВСЕГДА «вэ-». У обычного chat таких указаний три; его обработчик ответа уже сохраняет «у-», исправлять его повторно не нужно.
- Реальный cleanHebrewForSpeech меняет `וּגְבִינָה` в `וֶגְבִינָה`, а `לֶחֶם וּגְבִינָה` в `לֶחֶם וֶגְבִינָה`. speakHebrew передаёт этот результат в SpeechSynthesisUtterance и fallback. Это подтверждённая порча текста перед TTS, не измерение физического звука.

Основная норма и прочитанный официальный источник: language-standard.md, страница 41 материала Минобразования. Сохранять заданное нормативное /u/; не подменять его утверждением о якобы библейском произношении.

## Границы

1. Убрать из промптов chat/phone безусловное «всегда вэ» и запрет «у». Задать соответствие транскрипции конкретной нормативной огласовке: `וּ` → у-, `וְ` → вэ-; другие случаи не выводить по одной кириллической строке. Не превращать все союзы в у- и не выдавать модель за источник нормы.
2. Телефонный обработчик сохраняет нормативную транскрипцию в главной реплике и подсказках обеих веток провайдеров. Сохранить безопасную очистку строк и существующий формат ответа; не расширять до переписывания нормализатора всего приложения.
3. Удалить принудительное переогласование начальной ו в `fixHebrewPhonetics`. Сохранить исходные огласовки, прочие фонетические исправления, очистку меток, работу очереди, скорость, отмену, ошибки и fallback озвучки. Не изобретать огласовки по неогласованному тексту. Проверить также слова, где ו — часть корня, чтобы не принять союз за согласную и наоборот.
4. Не менять уроки, схемы данных, API-авторизацию, бюджеты, выбор провайдера, оценивание, переключение пола и принятую практику урока 5. Ошибку пола обычных sampleAnswers исправляет отдельная задача. Модели, ключи, продакшен и настройки голосов не менять.

## Критерии приёмки

- Реальные POST chat и phone с перехватом внешнего fetch: обе ветки Groq/Gemini, основная реплика и suggestedReplies. Синтетическое у- сохраняется, обычное вэ- не заменяется в обратную сторону, trim и форматы работают. Исходящий промпт не содержит прежнего обобщения и содержит понятное положительное правило.
- Реальный `speakHebrew` со stub браузерного аудиовыхода передаёт правильный текст в SpeechSynthesisUtterance; отдельно проверен путь fallback без сети и звука. Указать, какие ветки действительно исполнены. Не считать проверку строки прослушиванием TTS.
- Примеры минимум: союз в начале и середине фразы, пробелы/кавычки, заданное וּ/וְ, слово вроде וִילוֹן, неизменные соседние фонетические правила. Новые тесты должны падать на фактических старых модулях, подставленных в процессе, без копирования ошибочной формулы в отдельный всегда зелёный тест.
- Обязательные проверки DEVELOPMENT.md и независимое сравнение lint изменённых файлов с базой. Прежний долг не скрывать; не добавлять отключение правил ради новых нарушений.

Результат: project-control/results/runtime-normative-vav-result.md основного проекта, точный commit, границы diff, реальные команды/коды, источники и ограничения. Код и тесты только в отдельной рабочей копии. Без push/merge/выпуска, рабочей БД и платного ИИ. Подлинное качество произношения голосовых движков остаётся отдельной задачей.

## Завершение

Принято на b2dd5f2 и включено локально около 05:10. Ревью results/runtime-normative-vav-review.md. Повторная передача не нужна.
