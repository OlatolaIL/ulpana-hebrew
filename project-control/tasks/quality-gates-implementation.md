# Gemini: обязательные проверки качества

Статус: подготовлено архитектором 14 сентября 2026. НЕ отправлено. Публикация и изменение настроек GitHub этим файлом не разрешены. Сначала завершить уже назначенную задачу ручных языковых данных; назначение этой работы выполняется отдельной передачей.

## Проблема и результат

По results/quality-gates-review.md GitHub main не защищена, workflow и запусков нет. Локальный beta-checks.yml подготовлен, но не отслеживается Git. Нужно подготовить воспроизводимые обязательные проверки и доказать, что их ошибка блокирует принятый путь слияния/выпуска.

Прочитать общие AGENTS.md/DEVELOPMENT.md, beta-acceptance.md и независимое ревью. Работать в отдельной назначенной ветке/копии от актуальной принятой базы. Не переносить неподтверждённый кандидат 5383 и не включать чужие документы/секреты. Название проекта — «Ульпан Алеф».

## Локальный этап

1. Проверить существующий локальный workflow; подготовить минимальный patch только для CI и относящихся к нему проверок. Учитывать, что .github/ сейчас не отслеживается и существующее содержимое нельзя незаметно заменить. Не переписывать приложение и не обновлять зависимости без отдельной причины.
2. Node 24 и npm ci из lockfile; типы, обычные тесты, изолированные DB-тесты, сборка, audit high и lint AuthModal.tsx, FlashcardTrainer/FlashcardTrainer.tsx, LessonAiChat/useAiChat.ts обязательны. Прежний полный lint-долг видим отдельно; новые/изменённые файлы не должны добавлять нарушения. Не скрывать ошибки через continue-on-error обязательных шагов или --if-present.
3. Тестовая PostgreSQL только отдельный loopback:55433/ulpana_beta_test. Не использовать secrets боевой БД, платного ИИ, Telegram или хранилища. API-тесты используют синтетические credentials и перехват провайдера.
4. Удалённые actions должны иметь проверенные версии; рассмотренные SHA checkout/setup-node находятся в evidence/github-gates-readonly.json. При фиксации SHA указывать соответствующий тег комментарием и не заявлять неизменяемость тега.
5. Минимальные permissions, persist-credentials false. Логи не печатают env, полные remote URLs, JWT, PAT и конфигурацию секретов. Новый локальный credential не создавать и существующий не отзывать.
6. Обязательные jobs имеют стабильные уникальные имена и не пропускаются из-за необоснованных path/conditional фильтров. Если выбран merge queue, нужен соответствующий merge_group trigger; не включать очередь без принятого решения. Shallow checkout не должен ломать постоянные тесты: отрицательные испытания исторических модулей остаются отдельными доказательствами ревью, а не всегда зелёными тестами старых ошибок в каждом CI.
7. Проверить доступными локальными способами синтаксис и соответствие команд. Отдельно указать, что невозможно доказать без реального GitHub run. Не подменять удалённый результат локальным.

## Отдельный этап после разрешённой публикации

- Опубликовать только рассмотренный кандидат и получить реальный run для его SHA. Записать ссылку, SHA, job names, коды и результаты шагов.
- Настроить подходящую защиту main/required checks с учётом доступных GitHub прав и выбранного пути слияния. Не ослаблять защиту ради зелёного статуса. Уточнить, у кого остаётся обход и как исключается непроверенный выпуск.
- Контролируемо подтвердить блокировку на непродуктивном проверочном PR при ошибке обязательного check; затем исправить и получить успех. Не намеренно ломать main/production и не создавать публичные тестовые действия до разрешения.
- Подтвердить новый SHA после любого исправления; старый успешный run не принимается за новый. Не утверждать, что main protection контролирует отдельный прямой Vercel deployment, без проверки связи.

## Отчёт

project-control/results/quality-gates-implementation-result.md: база/commit, границы diff, локальные результаты и ограничения, точные удалённые доказательства только при выполненном втором этапе. Разделить «workflow подготовлен», «run выполнен», «ограничение настроено», «блокировка доказана». Полная коммерческая готовность из них не следует. Без отдельного разрешения остановиться после конкретного рассмотренного локального кандидата; не спрашивать пользователя о рутинных технических шагах.
